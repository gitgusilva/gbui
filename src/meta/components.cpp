// GENERATED by tools/generate_meta.py — do not edit.
//
// One entry per component, read out of include/gbui/widgets/*.hpp. Regenerate
// after adding a component or an option; CI checks that this file matches the
// headers, so a stale table fails the build rather than misleading a tool.
#include "gbui/meta/components.hpp"

namespace gbui::meta {
namespace {

std::vector<ComponentInfo> build() {
    std::vector<ComponentInfo> out;
    out.reserve(61);

    out.push_back(ComponentInfo{
        "badge",
        "Elements",
        "gbui/widgets/badge.hpp",
        "",
        "A pill — a branch name, a counter, a status chip.",
        "BadgeOptions",
        {
            PropertyInfo{"background", PropertyKind::Token, "Token", "Token::BgOverlay", {}, "", false},
            PropertyInfo{"foreground", PropertyKind::Token, "Token", "Token::TextMuted", {}, "The text on the wash. Paired with `background`, and the contrast between the two is the caller's to get right — a pill is one node and there is nothing here that can check it.", false},
        },
        false,
        false,
        "NodeId badge(Ui& ui, std::string_view value, const BadgeOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "button",
        "Elements",
        "gbui/widgets/button.hpp",
        "Without an `Interaction` there is no pointer to start a ripple from, so this overload ignores `options.ripple`. Everything else is identical.",
        "A button, in the four variants the design system has.",
        "ButtonOptions",
        {
            PropertyInfo{"variant", PropertyKind::Enum, "ButtonVariant", "ButtonVariant::Secondary", {"Primary", "Secondary", "Ghost", "Danger"}, "Which of the four this is. `Secondary` by default, because a screen where everything is primary has no primary.", false},
            PropertyInfo{"leading", PropertyKind::Icon, "std::optional<Icon>", "", {}, "Drawn before the label, in the label's colour.", true},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"block", PropertyKind::Bool, "bool", "false", {}, "Stretches to fill the row it is in, the way a COMMIT button does.", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "0.0f", {}, "Zero takes the active design's control height.", false},
            PropertyInfo{"iconSize", PropertyKind::Number, "float", "0.0f", {}, "The glyph's size. Zero matches the label's, which is what a button with a label wants; one with only an icon in it usually wants more, since the glyph is the whole button rather than a mark beside a word.", false},
            PropertyInfo{"id", PropertyKind::Text, "std::string_view", "", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What a reader who cannot see it is told this button is called. The label, unless it is given — which leaves exactly one case where the caller must say something: **an icon-only button, which has no label to borrow.** Nothing is guessed from the glyph; a name inferred from `Icon::Trash` would be a guess the reader has no way to check, and \"button, button, button\" across a toolbar is the failure this exists to stop.", false},
            PropertyInfo{"ripple", PropertyKind::Bool, "std::optional<bool>", "", {}, "A circle of ink that grows from where the pointer went down. Unset — the normal case — asks the active `Design`: Material throws ink, the others change the surface. Set, it overrides that decision for this one button, which is the escape hatch and not the default. Either way it needs the overload below (the ripple starts at the *point* the press landed) and an `id`, since that is what the animation is keyed by.", true},
        },
        false,
        false,
        "NodeId button(Ui& ui, std::string_view text, const ButtonOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "button",
        "Elements",
        "gbui/widgets/button.hpp",
        "The same button, given what it needs to draw a press.",
        "A button, in the four variants the design system has.",
        "ButtonOptions",
        {
            PropertyInfo{"variant", PropertyKind::Enum, "ButtonVariant", "ButtonVariant::Secondary", {"Primary", "Secondary", "Ghost", "Danger"}, "Which of the four this is. `Secondary` by default, because a screen where everything is primary has no primary.", false},
            PropertyInfo{"leading", PropertyKind::Icon, "std::optional<Icon>", "", {}, "Drawn before the label, in the label's colour.", true},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"block", PropertyKind::Bool, "bool", "false", {}, "Stretches to fill the row it is in, the way a COMMIT button does.", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "0.0f", {}, "Zero takes the active design's control height.", false},
            PropertyInfo{"iconSize", PropertyKind::Number, "float", "0.0f", {}, "The glyph's size. Zero matches the label's, which is what a button with a label wants; one with only an icon in it usually wants more, since the glyph is the whole button rather than a mark beside a word.", false},
            PropertyInfo{"id", PropertyKind::Text, "std::string_view", "", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What a reader who cannot see it is told this button is called. The label, unless it is given — which leaves exactly one case where the caller must say something: **an icon-only button, which has no label to borrow.** Nothing is guessed from the glyph; a name inferred from `Icon::Trash` would be a guess the reader has no way to check, and \"button, button, button\" across a toolbar is the failure this exists to stop.", false},
            PropertyInfo{"ripple", PropertyKind::Bool, "std::optional<bool>", "", {}, "A circle of ink that grows from where the pointer went down. Unset — the normal case — asks the active `Design`: Material throws ink, the others change the surface. Set, it overrides that decision for this one button, which is the escape hatch and not the default. Either way it needs the overload below (the ripple starts at the *point* the press landed) and an `id`, since that is what the animation is keyed by.", true},
        },
        false,
        true,
        "NodeId button(Ui& ui, const Interaction& input, std::string_view text, const ButtonOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "checkbox",
        "Elements",
        "gbui/widgets/checkbox.hpp",
        "Returns true on the frame the user toggled it.",
        "A checkbox.",
        "CheckboxOptions",
        {
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"label", PropertyKind::Text, "std::string_view", "", {}, "Drawn to the right of the box; empty draws the box alone.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What a reader is told this is, when the label is not it. The label by default, which is right whenever there is one. Needed where the words are drawn *beside* the control rather than by it — a row that lays out its own caption and passes an empty `label` — because then there is nothing for the control to borrow and \"switch, on\" is all a reader gets.", false},
            PropertyInfo{"size", PropertyKind::Number, "float", "0.0f", {}, "Zero takes the active design's box size and corner.", false},
        },
        false,
        true,
        "[[nodiscard]] bool checkbox(Ui& ui, const Interaction& input, std::string_view id, bool checked, const CheckboxOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "field",
        "Elements",
        "gbui/widgets/field.hpp",
        "Draws the caption, then `control`, then the help or the error.",
        "A control with its label, its help and its error, laid out as one thing.",
        "FieldOptions",
        {
            PropertyInfo{"label", PropertyKind::Text, "std::string_view", "", {}, "The caption above the control. Empty draws none, which is what a field inside a table cell or beside its own heading wants.", false},
            PropertyInfo{"forId", PropertyKind::Text, "std::string_view", "", {}, "The control this labels, so clicking the caption focuses it. The id of the control built inside the callback — this cannot be worked out from the tree, because the field is built before the control is and a component never reaches into what a caller wrote.", false},
            PropertyInfo{"help", PropertyKind::Text, "std::string_view", "", {}, "Guidance under the control. Replaced by `error` while there is one: advice and a complaint in the same place, at the same time, is two things asking to be read first.", false},
            PropertyInfo{"error", PropertyKind::Text, "std::string_view", "", {}, "What is wrong. Non-empty puts the field in its invalid state — the message is drawn in the error colour and the caption goes with it. It does **not** restyle the control. A component does not reach into another component's options, and a border that changed colour by remote control would be exactly that; pass `invalid` to the control as well where it has one. What this owns is the message and the caption.", false},
            PropertyInfo{"required", PropertyKind::Bool, "bool", "false", {}, "Marks the caption with the usual asterisk.", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "5.0f", {}, "Between the caption, the control and the message.", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "0.0f", {}, "", false},
        },
        false,
        true,
        "FieldResult field(Ui& ui, const Interaction& input, std::string_view id, const FieldOptions& options, const std::function<void(Ui&)>& control);",
    });
    out.push_back(ComponentInfo{
        "hyperlink",
        "Elements",
        "gbui/widgets/hyperlink.hpp",
        "Returns true on the frame it was followed — by click, or by Return while focused. `hyperlink` rather than `link`, because `link` in a C++ codebase already means the linker, a linked list, or the act of linking; this is the one thing it does not usually mean.",
        "A hyperlink.",
        "HyperlinkOptions",
        {
            PropertyInfo{"href", PropertyKind::Text, "std::string_view", "", {}, "Where it points. Following it hands this to `openUrl`. Empty follows nothing and only reports the click, which is what a link that scrolls somewhere inside the application wants.", false},
            PropertyInfo{"chord", PropertyKind::Opaque, "Modifiers", "", {}, "Modifiers that must be held for a click to count. All false — the default — means a plain click follows it, which is what a link is. Requiring a chord is for a link inside editable text, where a plain click has to place the caret instead; that is the bargain every editor makes, and it is a decision about the surrounding document rather than about the link, so it is the caller's.", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"trailing", PropertyKind::Icon, "std::optional<Icon>", "", {}, "Drawn after the label — an arrow for \"this leaves the application\".", true},
            PropertyInfo{"color", PropertyKind::Token, "Token", "Token::Accent", {}, "", false},
            PropertyInfo{"size", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"underlineOnHover", PropertyKind::Bool, "bool", "false", {}, "Underlines always, or only under the pointer. Always is the accessible default: colour alone is not a cue for anyone who cannot see it.", false},
        },
        false,
        true,
        "[[nodiscard]] bool hyperlink(Ui& ui, const Interaction& input, std::string_view id, std::string_view label, const HyperlinkOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "icon",
        "Elements",
        "gbui/widgets/icon.hpp",
        "",
        "One icon from the built-in Lucide set.",
        "IconOptions",
        {
            PropertyInfo{"color", PropertyKind::Token, "Token", "Token::Text", {}, "", false},
            PropertyInfo{"size", PropertyKind::Number, "float", "16.0f", {}, "Side of the square the icon is drawn in.", false},
            PropertyInfo{"stroke", PropertyKind::Number, "float", "2.0f", {}, "Stroke width on Lucide's 24-unit grid; scaled with the icon.", false},
        },
        false,
        false,
        "NodeId icon(Ui& ui, Icon which, const IconOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "image",
        "Elements",
        "gbui/widgets/image.hpp",
        "Draws `source` inside a box. The picture is sampled bilinearly and cut by the same rounded corners the boxes use, so an image in a card and the card agree about the curve. The pixels are **borrowed for the frame**: they are read when the frame is painted, not when this is called, and nothing here copies them. That is the same contract a label's `string_view` has, and the same one it breaks in the same way — a buffer that dies at the end of the enclosing scope is a picture of whatever the stack holds by the time anyone looks.",
        "A picture in a box — HTML's `<img>`, with the parts of it that are a toolkit's business.",
        "ImageOptions",
        {
            PropertyInfo{"width", PropertyKind::Length, "Length", "kAuto", {}, "Its box. Either left `kAuto` takes the picture's own size in that direction, so an image with neither set is drawn at 1:1.", false},
            PropertyInfo{"height", PropertyKind::Length, "Length", "kAuto", {}, "", false},
            PropertyInfo{"fit", PropertyKind::Opaque, "ImageFit", "ImageFit::Contain", {}, "How the picture meets a box that is not its shape.", false},
            PropertyInfo{"radius", PropertyKind::Number, "float", "0.0f", {}, "Rounded corners, cut from the picture itself — an avatar is this and a radius of half its size.", false},
            PropertyInfo{"opacity", PropertyKind::Number, "float", "1.0f", {}, "", false},
            PropertyInfo{"background", PropertyKind::Opaque, "std::optional<Fill>", "", {}, "Behind the picture, which is what shows through a transparent one and what fills the letterboxing `Contain` leaves.", true},
            PropertyInfo{"padding", PropertyKind::Edges, "Edges", "", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "0.0f", {}, "Kept out of the flow's stretch, like an icon: a picture has a size and a container has no business overriding it.", false},
            PropertyInfo{"shrink", PropertyKind::Number, "float", "0.0f", {}, "", false},
            PropertyInfo{"alt", PropertyKind::Text, "std::string_view", "", {}, "Drawn instead when there are no pixels — HTML's `alt`, doing the job `alt` actually does in a browser rather than the one it is credited with. An empty one leaves the box empty, which is right for decoration. A missing logo in a list of companies is not decoration, and a row that silently loses its mark is worse than one showing three letters.", false},
        },
        false,
        false,
        "NodeId image(Ui& ui, const Bitmap& source, const ImageOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "label",
        "Elements",
        "gbui/widgets/label.hpp",
        "Returns the id to focus when the label was clicked, or nothing. The caller hands it to `Interaction::focus`, which keeps the toolkit from mutating interaction state behind a component's back: if (const auto target = label(ui, input, \"f.name\", \"Repository\", {.forId = \"name\"})) interaction.focus(*target);",
        "A caption for a control.",
        "LabelOptions",
        {
            PropertyInfo{"forId", PropertyKind::Text, "std::string_view", "", {}, "The control this names. Clicking the label focuses it, exactly as `<label for>` does, unless that control is disabled or read-only.", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"readOnly", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"color", PropertyKind::Token, "Token", "Token::TextMuted", {}, "", false},
            PropertyInfo{"size", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"required", PropertyKind::Bool, "bool", "false", {}, "Marks the field as required, with the usual asterisk.", false},
        },
        false,
        true,
        "std::optional<std::string_view> label(Ui& ui, const Interaction& input, std::string_view id, std::string_view text, const LabelOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "progressBar",
        "Elements",
        "gbui/widgets/progress.hpp",
        "Not an input, but it belongs with them: the same track, the same tokens.",
        "A progress bar, determinate or not.",
        "ProgressOptions",
        {
            PropertyInfo{"value", PropertyKind::Number, "double", "0.0", {}, "Below zero draws the indeterminate form.", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "6.0f", {}, "", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "1.0f", {}, "", false},
            PropertyInfo{"color", PropertyKind::Token, "Token", "Token::Accent", {}, "", false},
            PropertyInfo{"phase", PropertyKind::Number, "float", "0.0f", {}, "Phase for the indeterminate form, in turns. Feed it a clock.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What is progressing — \"Cloning\", \"Uploading\". A bar with no name is a percentage with nothing attached to it.", false},
        },
        false,
        false,
        "NodeId progressBar(Ui& ui, const ProgressOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "radio",
        "Elements",
        "gbui/widgets/radio.hpp",
        "Returns true on the frame it was chosen; a radio that is already selected reports nothing, because choosing it again is not a change.",
        "One option of a group.",
        "RadioOptions",
        {
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"label", PropertyKind::Text, "std::string_view", "", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What a reader is told this is, when the label is not it. The label by default, which is right whenever there is one. Needed where the words are drawn *beside* the control rather than by it — a row that lays out its own caption and passes an empty `label` — because then there is nothing for the control to borrow and \"switch, on\" is all a reader gets.", false},
            PropertyInfo{"size", PropertyKind::Number, "float", "0.0f", {}, "Zero takes the active design's size.", false},
        },
        false,
        true,
        "bool radio(Ui& ui, const Interaction& input, std::string_view id, bool selected, const RadioOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "select",
        "Elements",
        "gbui/widgets/select.hpp",
        "A closed box that opens a list. Closed and focused, Up and Down step the value and Return or Space opens the list. Open, they walk the highlight, Home and End jump to the ends, Return or Space commits, and Escape closes without changing anything. The list keeps the highlighted row in view as it moves, and the box — not the rows — keeps the keyboard, so Tab leaves the control rather than walking into the popup.",
        "A closed box that opens a list.",
        "SelectOptions",
        {
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What this is called, for a reader who cannot see the caption beside it. Unnecessary when a `label` or a `field` names it — those attach the relation, and a name given twice is a name read out twice. Necessary the rest of the time, and the placeholder is not a substitute: a box named by its placeholder loses its name the moment somebody types in it.", false},
            PropertyInfo{"placeholder", PropertyKind::Text, "std::string_view", "\"Select…\"", {}, "", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "0.0f", {}, "", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "0.0f", {}, "Zero takes the active design's control height, so a select lines up with every other control on its row without anyone matching numbers.", false},
            PropertyInfo{"maxVisible", PropertyKind::Number, "std::size_t", "12", {}, "How many rows fit before the list scrolls. The rest are reachable by scrolling or by walking to them with the arrow keys.", false},
            PropertyInfo{"maxListHeight", PropertyKind::Number, "float", "kAuto", {}, "A hard ceiling in pixels, which wins over `maxVisible` when both would apply — a row count cannot know how tall the window is. `kAuto` leaves the row count in charge.", false},
            PropertyInfo{"listScroll", PropertyKind::Enum, "ScrollAxis", "ScrollAxis::Vertical", {"None", "Vertical", "Horizontal"}, "Whether the open list scrolls at all. `None` clips it instead, for a caller that would rather constrain the list than let it move.", false},
        },
        false,
        true,
        "[[nodiscard]] SelectResult select(Ui& ui, const Interaction& input, std::string_view id, const std::vector<std::string>& items, std::optional<std::size_t> selected, SelectState& state, const SelectOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "slider",
        "Elements",
        "gbui/widgets/slider.hpp",
        "Follows the pointer while it is held, even when it wanders off the track — which is what `Interaction::dragging` is for.",
        "A single-value slider.",
        "SliderOptions",
        {
            PropertyInfo{"minimum", PropertyKind::Number, "double", "0.0", {}, "", false},
            PropertyInfo{"maximum", PropertyKind::Number, "double", "1.0", {}, "", false},
            PropertyInfo{"step", PropertyKind::Number, "double", "0.0", {}, "0 is continuous; anything else snaps to a multiple of it.", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "1.0f", {}, "Takes the free space on the parent's main axis, which is what a slider in a row wants and the only thing `grow` can mean. In a *column* the main axis is vertical, so this used to make a slider eat the leftover height and sit alone at the bottom of its card. It no longer can: the control is clamped to `height` whichever way it is pointed, so growing is free horizontally and impossible vertically.", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "20.0f", {}, "", false},
            PropertyInfo{"showValue", PropertyKind::Bool, "bool", "false", {}, "Shows the value at the right of the track.", false},
            PropertyInfo{"decimals", PropertyKind::Number, "int", "2", {}, "Digits in the label `showValue` draws. It does **not** round the value: the caller's number is untouched, and a slider that quietly rounded it would be one you cannot set to a third.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What this slider is called, for a reader who cannot see the caption beside it. Unnecessary when a `label` or a `field` names it — those attach the relation instead.", false},
            PropertyInfo{"valueText", PropertyKind::Text, "std::string_view", "", {}, "What a reader hears instead of the number — \"70 percent\", \"3 of 8\". Empty falls back to the number, which is right for a bare fraction and wrong for everything with a unit. The toolkit cannot supply this: it has no locale to spell a unit in and no idea what the number counts.", false},
        },
        false,
        true,
        "[[nodiscard]] SliderResult slider(Ui& ui, const Interaction& input, std::string_view id, double value, const SliderOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "divider",
        "Elements",
        "gbui/widgets/spacing.hpp",
        "A hairline. Horizontal in a column, vertical in a row: pass the direction of the container it sits in.",
        "The two nodes that exist to take up room, or to refuse to.",
        "",
        {},
        false,
        false,
        "NodeId divider(Ui& ui, Direction containerDirection);",
    });
    out.push_back(ComponentInfo{
        "spacer",
        "Elements",
        "gbui/widgets/spacing.hpp",
        "Fills the free space on the main axis — the flexible gap between a title and the actions on the far side of a toolbar.",
        "The two nodes that exist to take up room, or to refuse to.",
        "",
        {},
        false,
        false,
        "NodeId spacer(Ui& ui, float grow = 1.0f);",
    });
    out.push_back(ComponentInfo{
        "emphasis",
        "Elements",
        "gbui/widgets/text.hpp",
        "",
        "Runs of text, and the two semantic shorthands over them.",
        "",
        {},
        false,
        false,
        "NodeId emphasis(Ui& ui, std::string_view value, TextOptions options = {});",
    });
    out.push_back(ComponentInfo{
        "richText",
        "Elements",
        "gbui/widgets/text.hpp",
        "A line of text whose runs differ — in colour, weight, slant or decoration. Built as a row of runs sharing a line, which is the honest shape given that a node carries one style. With `wrap` it becomes a *wrapping* row, so the line breaks between spans — see the caveat on that option. A paragraph that has to break mid-sentence still wants `text` with `TextOverflow::Wrap` and one colour, or a gradient across the whole run. Runs are centred against each other rather than sitting on a shared baseline, because `Align::Baseline` is not implemented yet. Spans of one size — the usual case — look identical either way.",
        "Runs of text, and the two semantic shorthands over them.",
        "RichTextOptions",
        {
            PropertyInfo{"align", PropertyKind::Opaque, "TextAlign", "TextAlign::Start", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "0.0f", {}, "", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "0.0f", {}, "Space between spans. Zero butts them together, which is what a sentence wants; the spaces come from the spans themselves.", false},
            PropertyInfo{"wrap", PropertyKind::Bool, "bool", "false", {}, "Lets the line wrap **between spans**. Not the same as wrapping text: the break can only fall where one span ends and the next begins, so a single long span still overflows its line rather than breaking inside itself. Splitting a sentence into more spans gives the layout more places to break. Real wrapping through mixed runs needs an inline formatting context, which the engine does not have.", false},
        },
        false,
        false,
        "NodeId richText(Ui& ui, const std::vector<TextSpan>& spans, const RichTextOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "sectionHeading",
        "Elements",
        "gbui/widgets/text.hpp",
        "Small, uppercase, muted: the \"UNSTAGED (1)\" kind of heading.",
        "Runs of text, and the two semantic shorthands over them.",
        "",
        {},
        false,
        false,
        "NodeId sectionHeading(Ui& ui, std::string_view value);",
    });
    out.push_back(ComponentInfo{
        "strong",
        "Elements",
        "gbui/widgets/text.hpp",
        "Semantic shorthands, in the sense HTML gives them: `strong` is importance and `emphasis` is stress, and each happens to be drawn with a weight or a slant. They exist so a call site says what it means rather than how it looks, which is what lets the look change later.",
        "Runs of text, and the two semantic shorthands over them.",
        "",
        {},
        false,
        false,
        "NodeId strong(Ui& ui, std::string_view value, TextOptions options = {});",
    });
    out.push_back(ComponentInfo{
        "text",
        "Elements",
        "gbui/widgets/text.hpp",
        "A run of text with the theme's colours applied — the common case that would otherwise be two structs at every call site.",
        "Runs of text, and the two semantic shorthands over them.",
        "TextOptions",
        {
            PropertyInfo{"color", PropertyKind::Token, "Token", "Token::Text", {}, "", false},
            PropertyInfo{"weight", PropertyKind::Opaque, "FontWeight", "FontWeight::Regular", {}, "", false},
            PropertyInfo{"slant", PropertyKind::Opaque, "FontSlant", "FontSlant::Normal", {}, "", false},
            PropertyInfo{"role", PropertyKind::Opaque, "FontRole", "FontRole::Ui", {}, "", false},
            PropertyInfo{"size", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"align", PropertyKind::Opaque, "TextAlign", "TextAlign::Start", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "0.0f", {}, "Give the run the leftover space on the main axis. A commit subject wants this: it takes what is left after the fixed columns and elides the rest, instead of pushing them off the row.", false},
            PropertyInfo{"overflow", PropertyKind::Opaque, "TextOverflow", "TextOverflow::Ellipsis", {}, "", false},
            PropertyInfo{"maxLines", PropertyKind::Number, "int", "0", {}, "With `overflow = Wrap`, how many lines the run may take. Zero is unlimited; a count clamps it and ellipsises the last line.", false},
            PropertyInfo{"lineHeight", PropertyKind::Number, "float", "0.0f", {}, "A multiplier over the font size. Zero takes the face's own line height, which is tight for a paragraph and right for a single-line label.", false},
            PropertyInfo{"underline", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"strikeThrough", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"gradient", PropertyKind::Opaque, "Gradient", "", {}, "A gradient across the run, instead of `color`. Two stops is the common case: `Gradient::linear(Fill{Token::Accent}, Fill{Token::Text}, 90)`.", false},
        },
        false,
        false,
        "NodeId text(Ui& ui, std::string_view value, const TextOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "textInput",
        "Elements",
        "gbui/widgets/textInput.hpp",
        "Edits `state` in place when it has focus and returns what happened, so a caller can react to a submit without diffing the text. The pointer places the caret: a press puts it at the character it landed on, and holding and moving drags a selection out from there. Both measure the run the same way the caret is drawn, so what is clicked is where it lands.",
        "A single-line input, and a type that says what it accepts.",
        "TextInputOptions",
        {
            PropertyInfo{"type", PropertyKind::Enum, "InputType", "InputType::Text", {"Text", "Password", "Number"}, "What the box accepts. Everything below marked for one type is ignored by the others.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What this is called, for a reader who cannot see the caption beside it. Unnecessary when a `label` or a `field` names it — those attach the relation, and a name given twice is a name read out twice. Necessary the rest of the time, and the placeholder is not a substitute: a box named by its placeholder loses its name the moment somebody types in it.", false},
            PropertyInfo{"placeholder", PropertyKind::Text, "std::string_view", "", {}, "Shown while the text is empty. It steps aside once the box has the keyboard: a hint you are being asked to type over is noise the moment you start.", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"readOnly", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"invalid", PropertyKind::Bool, "bool", "false", {}, "Draws the box in the error colour. The control's half of the state `field` owns the message for. A component does not reach into another component's options, so a field with an error on it does not restyle its control by remote control — the caller says so twice, once in each place, and both are readable on their own.", false},
            PropertyInfo{"leading", PropertyKind::Icon, "std::optional<Icon>", "", {}, "A glyph at the leading edge, inside the box.", true},
            PropertyInfo{"revealToggle", PropertyKind::Bool, "bool", "true", {}, "The eye at the trailing edge that shows the text while it is held. On by default, because a field you cannot read back is a field people mistype into; turn it off where a shoulder-surfer is the threat being designed against.", false},
            PropertyInfo{"revealed", PropertyKind::Bool, "bool", "false", {}, "Whether the text is currently shown. The caller owns it, like every other piece of state here, and flips it when the result says so.", false},
            PropertyInfo{"minimum", PropertyKind::Number, "double", "-1e18", {}, "", false},
            PropertyInfo{"maximum", PropertyKind::Number, "double", "1e18", {}, "", false},
            PropertyInfo{"step", PropertyKind::Number, "double", "1.0", {}, "What one arrow, one wheel notch or one step button is worth.", false},
            PropertyInfo{"decimals", PropertyKind::Number, "int", "0", {}, "Digits after the point. Zero also stops a point being typed at all.", false},
            PropertyInfo{"suffix", PropertyKind::Text, "std::string_view", "", {}, "Drawn after the value, and never part of it: `\" min\"`, `\"px\"`, `\"%\"`. A suffix inside the editable text would be a suffix the caret can be put in the middle of.", false},
            PropertyInfo{"steppers", PropertyKind::Enum, "StepperPlacement", "StepperPlacement::Sides", {"Sides", "Stacked", "None"}, "Where the step buttons go, and whether there are any at all.", false},
            PropertyInfo{"stackedBelow", PropertyKind::Number, "float", "110.0f", {}, "Below this width the steppers drop to `Stacked` automatically, so a box in a narrow column shows its value rather than two buttons.", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "0.0f", {}, "Zero takes the active design's control height.", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "kAuto", {}, "`kAuto` fits the content — except on a number, which takes 120 px: a box sized to its digits changes width as they are typed, and the steppers walk out from under the pointer clicking them.", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "0.0f", {}, "", false},
        },
        false,
        true,
        "TextInputResult textInput(Ui& ui, const Interaction& input, std::string_view id, TextEditState& state, const TextInputOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "textarea",
        "Elements",
        "gbui/widgets/textarea.hpp",
        "Edits `state` in place when it has focus, and reports what happened. `submitted` is the modified Return; `changed` is any edit at all. The component never writes to your model — the text in `state` is yours and this is the only thing that touches it. The view follows the caret: typing at the bottom of a full box scrolls it, and so does moving there with the arrows. Nothing else moves it on its own, so a reader who has scrolled up to check something stays where they put themselves until they type again.",
        "A multi-line plain text box.",
        "TextareaOptions",
        {
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What this is called, for a reader who cannot see the caption beside it. Unnecessary when a `label` or a `field` names it — those attach the relation, and a name given twice is a name read out twice. Necessary the rest of the time, and the placeholder is not a substitute: a box named by its placeholder loses its name the moment somebody types in it.", false},
            PropertyInfo{"placeholder", PropertyKind::Text, "std::string_view", "", {}, "", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"readOnly", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"rows", PropertyKind::Number, "int", "4", {}, "How many lines tall the box is, before anything is typed. A count of lines rather than a height in pixels, because that is the unit the answer is in — \"about four lines\" survives a change of type size and \"96 px\" does not.", false},
            PropertyInfo{"maxRows", PropertyKind::Number, "int", "0", {}, "Grows with the content, to this many lines, then scrolls. Zero keeps the box at `rows` from the first frame, which is what a form with a fixed layout wants. A chat composer or a commit message box wants this set: the box starts small and opens up as the writing goes on, which is the behaviour everyone now expects and nobody asks for.", false},
            PropertyInfo{"submitOnModifiedReturn", PropertyKind::Bool, "bool", "true", {}, "Ctrl+Return — Cmd+Return on macOS — reports `submitted`. Return itself belongs to the text.", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "0.0f", {}, "", false},
        },
        false,
        true,
        "TextEditResult textarea(Ui& ui, const Interaction& input, std::string_view id, TextareaState& state, const TextareaOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "toggle",
        "Elements",
        "gbui/widgets/toggle.hpp",
        "Same contract as the checkbox; the difference is only that a switch reads as \"on now\" and a checkbox as \"will apply\".",
        "A switch.",
        "ToggleOptions",
        {
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"label", PropertyKind::Text, "std::string_view", "", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What a reader is told this is, when the label is not it. The label by default, which is right whenever there is one. Needed where the words are drawn *beside* the control rather than by it — a row that lays out its own caption and passes an empty `label` — because then there is nothing for the control to borrow and \"switch, on\" is all a reader gets.", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "0.0f", {}, "Zero takes the active design's track and thumb.", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "0.0f", {}, "", false},
        },
        false,
        true,
        "[[nodiscard]] bool toggle(Ui& ui, const Interaction& input, std::string_view id, bool on, const ToggleOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "box",
        "Containers",
        "gbui/widgets/box.hpp",
        "Opens a container. Everything until the returned scope dies is inside it.",
        "The general container, the way `<div>` is one.",
        "BoxOptions",
        {
            PropertyInfo{"direction", PropertyKind::Opaque, "Direction", "Direction::Row", {}, "", false},
            PropertyInfo{"justify", PropertyKind::Opaque, "Justify", "Justify::Start", {}, "", false},
            PropertyInfo{"align", PropertyKind::Opaque, "Align", "Align::Stretch", {}, "", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "0.0f", {}, "", false},
            PropertyInfo{"padding", PropertyKind::Edges, "Edges", "", {}, "", false},
            PropertyInfo{"margin", PropertyKind::Edges, "Edges", "", {}, "", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"minWidth", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"minHeight", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"maxWidth", PropertyKind::Number, "float", "std::numeric_limits<float>::infinity()", {}, "", false},
            PropertyInfo{"maxHeight", PropertyKind::Number, "float", "std::numeric_limits<float>::infinity()", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "0.0f", {}, "", false},
            PropertyInfo{"shrink", PropertyKind::Number, "float", "1.0f", {}, "", false},
            PropertyInfo{"basis", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"background", PropertyKind::Opaque, "Fill", "", {}, "", false},
            PropertyInfo{"backgroundGradient", PropertyKind::Opaque, "Gradient", "", {}, "", false},
            PropertyInfo{"border", PropertyKind::Token, "std::optional<Token>", "", {}, "", true},
            PropertyInfo{"borderWidth", PropertyKind::Number, "float", "1.0f", {}, "", false},
            PropertyInfo{"radius", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"opacity", PropertyKind::Number, "float", "1.0f", {}, "", false},
            PropertyInfo{"overflow", PropertyKind::Opaque, "Overflow", "Overflow::Visible", {}, "", false},
            PropertyInfo{"layer", PropertyKind::Opaque, "Layer", "Layer::Content", {}, "", false},
            PropertyInfo{"cursor", PropertyKind::Opaque, "Cursor", "Cursor::Default", {}, "", false},
            PropertyInfo{"id", PropertyKind::Text, "std::string_view", "", {}, "", false},
            PropertyInfo{"focusable", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"role", PropertyKind::Opaque, "Role", "Role::None", {}, "What this container is to a reader who cannot see it. `None` by default, and that is the right default for the general container: most boxes are layout, and a tree full of anonymous groups is a tree a reader has to walk through rather than one they can navigate. Set it where the box *is* something — a `Form`, a `List`, a named `Group`, a `Figure` around a drawing.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What it is called. A `Group` with no name is not worth announcing, and the tree treats it as though the role were `None`.", false},
        },
        true,
        false,
        "Ui::Scope box(Ui& ui, const BoxOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "listRow",
        "Containers",
        "gbui/widgets/listRow.hpp",
        "Opens a row. Returns a Scope, so its contents are written inside the braces. `selected` washes the row in the accent at 18%, `hovered` uses `surfaceHover`, and both are passed in — components hold no state.",
        "A row of a list: the sidebar's branches, the commit list, the changed files.",
        "ListRowOptions",
        {
            PropertyInfo{"selected", PropertyKind::Bool, "bool", "false", {}, "Passed in, never held. The row draws the state and the application owns it, which is the contract every component here has.", false},
            PropertyInfo{"hovered", PropertyKind::Bool, "bool", "false", {}, "As above. Separate from what the pointer is over, because a list often wants the keyboard's row lit instead.", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "28.0f", {}, "", false},
            PropertyInfo{"padding", PropertyKind::Edges, "Edges", "Edges::symmetric(0.0f, 12.0f)", {}, "", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "6.0f", {}, "", false},
            PropertyInfo{"id", PropertyKind::Text, "std::string_view", "", {}, "", false},
        },
        true,
        false,
        "Ui::Scope listRow(Ui& ui, const ListRowOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "marquee",
        "Containers",
        "gbui/widgets/marquee.hpp",
        "Draws `content` twice, side by side, sliding. Twice is the whole trick. One copy leaves a hole behind it while it travels; a second, exactly a content's width behind, fills that hole — and because the offset wraps at that same width, the seam between them never arrives at a moment the reader could see it. Anything narrower than the strip would show the gap on its own, which is what `gap` is measured from rather than added to. `delta` is the frame's own, and **zero stops it**. That is the one interaction a ticker has: held still while the pointer is over it, a reader can read a name instead of chasing it. Stopping is the caller's to decide because only the caller knows what should stop it. The content is built twice per frame, so it should be cheap — a row of labels, not a table. It is laid out in one line: this is a strip, and a strip that wrapped would be a paragraph that moves. Returns true on the frame it came round, which is the one moment a caller may change the content without the reader seeing it happen: the strip is showing the beginning of a pass, so a *different* pass beginning there reads as the next lap rather than as a jump. Changing it at any other time slides everything already on screen sideways by whatever was added or removed.",
        "A strip whose contents slide past and come round again.",
        "MarqueeOptions",
        {
            PropertyInfo{"speed", PropertyKind::Number, "float", "42.0f", {}, "Pixels a second. Negative runs the other way, which is what a right-to -left reader expects of the same strip.", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "0.0f", {}, "Between the end of one pass and the start of the next. Set it for content with no spacing of its own — a sentence, which would otherwise read as one long word repeated. Leave it at **zero** for content that already spaces itself, a row of chips or cards: this gap applies at the seam and nowhere else, so anything wider than what the items already put between them is a hole that comes round once a lap and is the one part of a marquee a reader will notice.", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "kAuto", {}, "The strip's own height. Auto takes the content's.", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "1.0f", {}, "Takes the space left in the row it is in, and does so by default. It has to be told. Both passes are out of the flow — that is how they slide — so the strip has no content to be measured from and would otherwise collapse to nothing and draw an empty band, which is exactly what it did.", false},
            PropertyInfo{"padding", PropertyKind::Edges, "Edges", "", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the strip is showing — \"Now playing\", \"Market\". The content itself reaches a reader through the tree; this says what the band is.", false},
        },
        false,
        true,
        "bool marquee(Ui& ui, const Interaction& input, std::string_view id, MarqueeState& state, float delta, const std::function<void(Ui&)>& content, const MarqueeOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "panel",
        "Containers",
        "gbui/widgets/panel.hpp",
        "",
        "A surface with a border and a radius — a card, a dialog body, a docked pane.",
        "PanelOptions",
        {
            PropertyInfo{"background", PropertyKind::Token, "Token", "Token::BgElevated", {}, "", false},
            PropertyInfo{"border", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"padding", PropertyKind::Edges, "Edges", "Edges::all(12.0f)", {}, "", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "8.0f", {}, "", false},
            PropertyInfo{"direction", PropertyKind::Opaque, "Direction", "Direction::Column", {}, "", false},
            PropertyInfo{"radius", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "0.0f", {}, "Takes the space left on the main axis, the way a pane holding a list has to. It starts from nothing, as `text` and the scroll view do, so it cannot push its siblings out of the column.", false},
        },
        true,
        false,
        "Ui::Scope panel(Ui& ui, const PanelOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "scrollArea",
        "Containers",
        "gbui/widgets/scroll.hpp",
        "Opens a scroll container. The content is laid out at its natural size and clipped to the viewport, and the offset moves it. Wheel over the container scrolls it; Page Up, Page Down, Home and End do too when it has focus; the bar can be dragged. Everything drawn inside costs a node, so this is a *scrolling* view and not a *virtualised* one — 50 000 rows here really do build 50 000 nodes. When the rows are uniform, `virtualList` builds only the visible ones — see virtualList.hpp.",
        "A scroll container.",
        "ScrollOptions",
        {
            PropertyInfo{"direction", PropertyKind::Opaque, "Direction", "Direction::Column", {}, "", false},
            PropertyInfo{"axis", PropertyKind::Enum, "std::optional<ScrollAxis>", "", {"None", "Vertical", "Horizontal"}, "Which way it scrolls. Auto follows `direction`, which is what a column of rows or a row of chips almost always wants.", true},
            PropertyInfo{"step", PropertyKind::Number, "float", "48.0f", {}, "Pixels per wheel notch.", false},
            PropertyInfo{"scrollbar", PropertyKind::Bool, "bool", "true", {}, "Draws the bar. Turning it off leaves wheel and keyboard scrolling.", false},
            PropertyInfo{"scrollbarWidth", PropertyKind::Number, "float", "10.0f", {}, "", false},
            PropertyInfo{"autoHideScrollbar", PropertyKind::Bool, "bool", "true", {}, "Hides the bar when the content fits, the way a desktop does.", false},
            PropertyInfo{"focusable", PropertyKind::Bool, "bool", "true", {}, "Whether Tab can land on the viewport. A pane the reader scrolls for itself wants that; a list inside a control that already holds the keyboard — a select — does not, or Tab walks into the popup.", false},
            PropertyInfo{"padding", PropertyKind::Edges, "Edges", "", {}, "", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "0.0f", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "1.0f", {}, "", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"minWidth", PropertyKind::Number, "float", "kAuto", {}, "Bounds on the viewport itself. A maximum is what turns \"as tall as its content\" into \"as tall as its content, then scroll\" — which is the rule a dropdown needs and could not express before.", false},
            PropertyInfo{"maxWidth", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"minHeight", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"maxHeight", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the region is, for a reader who cannot see a scrollbar. Only used when `focusable`, since that is the only time the region is a place the keyboard can be.", false},
        },
        true,
        true,
        "Ui::Scope scrollArea(Ui& ui, const Interaction& input, std::string_view id, ScrollState& state, const ScrollOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "scrollbar",
        "Containers",
        "gbui/widgets/scroll.hpp",
        "Draws the bar for a scroll view that is not where the bar belongs. A bar normally lives inside the view it drives, and `scrollArea` puts it there. A table is the case that forced this one out: its rows scroll vertically *inside* a box that scrolls horizontally, so the row view's own right-hand edge is out at the end of the widest column, and the bar drawn against it is a bar the reader has to scroll sideways to find. A browser has no such problem, because both bars belong to the visible box; this is how a caller says the same thing here. `box` is in the current container's coordinates — where the bar should be, not where the content is. The ids are the view's own, so the press, the drag and the paging are still handled by `scrollArea`: this draws, it does not behave. Turn the view's own bar off with `ScrollOptions::scrollbar` when you use it, or there will be two.",
        "A scroll container.",
        "",
        {},
        false,
        true,
        "void scrollbar(Ui& ui, const Interaction& input, std::string_view id, const ScrollState& state, Rect box, ScrollAxis axis = ScrollAxis::Vertical, float width = 10.0f, bool autoHide = true);",
    });
    out.push_back(ComponentInfo{
        "table",
        "Containers",
        "gbui/widgets/table.hpp",
        "Draws a table and reports what the reader did to it. `cell` is called for each visible cell and builds whatever belongs there. It is given the row and the column; the box it draws into is already the right width, because **the widths are resolved once for the whole table** rather than negotiated per row. That is the difference between a table and a list of rows that happen to contain the same number of things. The application owns the data, the order and the selection. This owns the geometry.",
        "A table: columns that agree, a header that stays, and rows that virtualise.",
        "TableOptions",
        {
            PropertyInfo{"rowHeight", PropertyKind::Number, "float", "30.0f", {}, "", false},
            PropertyInfo{"headerHeight", PropertyKind::Number, "float", "32.0f", {}, "", false},
            PropertyInfo{"stickyHeader", PropertyKind::Bool, "bool", "true", {}, "The header stays put while the body scrolls under it.", false},
            PropertyInfo{"zebra", PropertyKind::Bool, "bool", "false", {}, "Alternating row shading. Off by default: with a hover and a selection already in play, a third band is usually noise.", false},
            PropertyInfo{"virtualise", PropertyKind::Bool, "bool", "true", {}, "Builds only the visible rows. Off draws them all, which is right for a table of twenty and wrong for one of fifty thousand.", false},
            PropertyInfo{"cellPadding", PropertyKind::Edges, "Edges", "Edges::symmetric(0.0f, 10.0f)", {}, "", false},
            PropertyInfo{"gridLines", PropertyKind::Number, "float", "1.0f", {}, "The hairline under the header, and the width of every other rule here.", false},
            PropertyInfo{"rowLines", PropertyKind::Bool, "bool", "false", {}, "A rule under every row. Off by default: on a table read top to bottom, the rows are already separated by their own alignment, and a line under each one is a lot of ink for no information. Worth turning on when the cells wrap to different heights, or when the reader is comparing across a row rather than down a column.", false},
            PropertyInfo{"columnLines", PropertyKind::Bool, "bool", "false", {}, "A rule between every column, in the header and in the body — which, together with `rowLines`, is a full grid.", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "1.0f", {}, "", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the table is of — \"Commits by author\". A grid of numbers with no name is the hardest thing on a screen to make sense of by ear.", false},
        },
        false,
        true,
        "TableResult table(Ui& ui, const Interaction& input, std::string_view id, const std::vector<Column>& columns, std::size_t rowCount, TableState& state, const std::function<void(Ui&, std::size_t row, std::size_t column)>& cell, const TableOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "tabPanels",
        "Containers",
        "gbui/widgets/tabs.hpp",
        "Builds the panel bodies for a tab strip. Separate from `tabs` because the two are rarely siblings: a vertical strip sits beside its panels and a horizontal one above them, and a caller often puts a toolbar or a splitter between them. An unselected panel is built inside a box of no height that clips, so it takes part in nothing the reader can see while still being *there* — which is the whole difference `lazy` turns off.",
        "A strip of tabs.",
        "TabPanelsOptions",
        {
            PropertyInfo{"lazy", PropertyKind::Bool, "bool", "false", {}, "Build only the selected panel, and skip the rest entirely. Off by default, which is what a hidden `<div>` does on the web: it is still in the document, so anything that measures it or looks it up finds it. Turning it on is cheaper — an unbuilt panel costs nothing to lay out or paint — and the price is that everything about the panels the reader is not looking at stops existing: `input.frameOf` on a control inside one returns nothing, and any component that places itself from last frame's geometry starts from scratch when its tab comes back. For a handful of pages the difference is not worth thinking about. For a window whose tabs each hold a table of fifty thousand rows it is the difference between usable and not.", false},
        },
        false,
        false,
        "void tabPanels(Ui& ui, std::size_t selected, const std::vector<std::function<void(Ui&)>>& panels, const TabPanelsOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "tabs",
        "Containers",
        "gbui/widgets/tabs.hpp",
        "Draws the strip and reports which tab the user chose, or nothing. Stateless like everything else: the caller owns the selected index and hands it in. The strip is one focusable stop — the ARIA \"roving tabindex\" pattern — so Tab moves *past* the whole strip rather than through every tab in it, and the arrow keys move between them once it has the keyboard. Not built yet, and named rather than half-done: an overflow menu for when the tabs do not fit (they currently shrink and elide), a close affordance, and reordering by drag.",
        "A strip of tabs.",
        "TabsOptions",
        {
            PropertyInfo{"orientation", PropertyKind::Enum, "TabsOrientation", "TabsOrientation::Horizontal", {"Horizontal", "Vertical"}, "A strip along the top or a rail down the side. Both arrow pairs step it either way, so the keys do not change when the layout does.", false},
            PropertyInfo{"thickness", PropertyKind::Number, "float", "36.0f", {}, "The strip's thickness: its height when horizontal, its width when vertical.", false},
            PropertyInfo{"itemHeight", PropertyKind::Number, "float", "34.0f", {}, "Vertical only — how tall each row is. Horizontal takes its tab height from `thickness`.", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "2.0f", {}, "", false},
            PropertyInfo{"indicator", PropertyKind::Bool, "bool", "true", {}, "The moving bar marking the active tab: under it when horizontal, down its leading edge when vertical. It slides between tabs when an animator is present and simply appears on the new one when there is not.", false},
            PropertyInfo{"indicatorWidth", PropertyKind::Number, "float", "2.0f", {}, "", false},
            PropertyInfo{"indicatorInset", PropertyKind::Number, "float", "0.0f", {}, "How far the indicator sits from the strip's trailing edge — under a horizontal strip, beside a vertical one. Zero puts it hard against the edge, which is what a strip with a rule under it wants: the bar and the rule are then the same line, one of them lit. A strip without a rule usually wants a pixel or two of air, or the bar reads as an underline attached to the word rather than as a marker under the tab.", false},
            PropertyInfo{"itemPadding", PropertyKind::Edges, "std::optional<Edges>", "", {}, "The space inside each tab. Unset takes a default that fits the orientation: horizontal tabs get room under the label so the indicator clears its descenders, and vertical rows keep a lane free on the leading edge so the label does not shift sideways when a tab becomes the active one. Here for the same reason `Style::padding` is on every node — a caller who disagrees with the spacing should be able to say so rather than work around it — and it is the one piece of a tab strip's spacing the caller could not reach.", true},
            PropertyInfo{"rule", PropertyKind::Bool, "bool", "true", {}, "A rule along the whole strip — under it, or beside it — so the strip reads as an edge rather than as floating text.", false},
        },
        false,
        true,
        "std::optional<std::size_t> tabs(Ui& ui, const Interaction& input, std::string_view id, const std::vector<TabItem>& items, std::size_t selected, const TabsOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "toolbar",
        "Containers",
        "gbui/widgets/toolbar.hpp",
        "",
        "A horizontal bar of actions.",
        "ToolbarOptions",
        {
            PropertyInfo{"height", PropertyKind::Number, "float", "40.0f", {}, "", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "8.0f", {}, "", false},
            PropertyInfo{"padding", PropertyKind::Edges, "Edges", "Edges::symmetric(0.0f, 12.0f)", {}, "", false},
            PropertyInfo{"background", PropertyKind::Token, "Token", "Token::BgElevated", {}, "", false},
            PropertyInfo{"bottomBorder", PropertyKind::Bool, "bool", "true", {}, "*Reserved, and currently draws nothing.** `Border` is all four edges, so a toolbar's rule has to be a sibling: write `divider(ui, Direction::Column)` after the toolbar until the primitive grows per-edge widths. Named rather than removed, because the option is the right shape and only the primitive is missing.", false},
        },
        true,
        false,
        "Ui::Scope toolbar(Ui& ui, const ToolbarOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "virtualList",
        "Containers",
        "gbui/widgets/virtualList.hpp",
        "A list that builds only what is on screen. The trick is not the clipping — the scroll view already clips. It is that the rows that are *not* visible are replaced by two spacers, one standing in for everything above the slice and one for everything below. The content is therefore the full height it would have been, so the scrollbar, `maxOffset` and Page Down all keep working on the real list, while the arena holds the forty rows a person can actually see. VirtualSlice shown = virtualList(ui, input, \"history\", state, {.count = commits.size(), .rowHeight = 28.0f}, [&](Ui& ui, std::size_t index) { listRow(ui, input, rowId(index), …); }); `row` is called once per visible index, in order, inside a box of exactly `rowHeight`. It must not open a scope it leaves open. The viewport is last frame's, like everything else that needs geometry before layout has run, so the first frame after a resize builds the previous size's slice — and overscan is what keeps that from showing.",
        "A list that builds only what is on screen.",
        "VirtualListOptions",
        {
            PropertyInfo{"count", PropertyKind::Number, "std::size_t", "0", {}, "How many rows exist. Only the visible ones are ever built.", false},
            PropertyInfo{"rowHeight", PropertyKind::Number, "float", "28.0f", {}, "Every row is exactly this tall. The list enforces it rather than trusting the caller: a row that laid itself out taller would slide the ones after it out of step with the scrollbar.", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "0.0f", {}, "", false},
            PropertyInfo{"padding", PropertyKind::Edges, "Edges", "", {}, "", false},
            PropertyInfo{"overscan", PropertyKind::Number, "std::size_t", "2", {}, "Rows built above and below the viewport. Two is enough to keep a fast drag from showing an edge, and cheap enough not to think about.", false},
            PropertyInfo{"step", PropertyKind::Number, "float", "48.0f", {}, "", false},
            PropertyInfo{"scrollbar", PropertyKind::Bool, "bool", "true", {}, "Whether the container underneath draws one.", false},
            PropertyInfo{"scrollbarWidth", PropertyKind::Number, "float", "10.0f", {}, "", false},
            PropertyInfo{"autoHideScrollbar", PropertyKind::Bool, "bool", "true", {}, "It fades out when nothing is pointing at it, the way an overlay bar does.", false},
            PropertyInfo{"focusable", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "1.0f", {}, "", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the list is of — \"Commits\". Every row reports its position out of `count`, so a reader hears \"3 of 50 000\" and not \"3 of the fourteen that happen to be built\".", false},
        },
        false,
        true,
        "VirtualSlice virtualList(Ui& ui, const Interaction& input, std::string_view id, ScrollState& state, const VirtualListOptions& options, const std::function<void(Ui&, std::size_t)>& row);",
    });
    out.push_back(ComponentInfo{
        "menuItem",
        "Overlays",
        "gbui/widgets/menu.hpp",
        "Returns true on the frame it was chosen.",
        "The rows inside a popover.",
        "MenuItemOptions",
        {
            PropertyInfo{"leading", PropertyKind::Icon, "std::optional<Icon>", "", {}, "", true},
            PropertyInfo{"shortcut", PropertyKind::Text, "std::string_view", "", {}, "Drawn right-aligned in the mono face, and drawn *only*: the row does not bind it. A menu is where a shortcut is advertised; the application is where it is handled.", false},
            PropertyInfo{"selected", PropertyKind::Bool, "bool", "false", {}, "The chosen value: a check mark and the strong text colour.", false},
            PropertyInfo{"checkSide", PropertyKind::Enum, "CheckSide", "CheckSide::Leading", {"Leading", "Trailing"}, "Where that check mark goes. Leading suits a menu, trailing a select.", false},
            PropertyInfo{"highlighted", PropertyKind::Bool, "bool", "false", {}, "Drawn as though the pointer were on it. That is what walking a list with the arrow keys looks like — the highlight moves and the pointer does not — and it is a different question from `selected`, which is the value the list would return.", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"danger", PropertyKind::Bool, "bool", "false", {}, "The destructive one, in the error colour — which is the only warning a reader gets before pressing it.", false},
            PropertyInfo{"focusable", PropertyKind::Bool, "bool", "true", {}, "Whether Tab can land on it. A list whose owner drives the highlight — a select — turns this off, so Tab leaves the control instead of walking into an open popup and back out of the window's order.", false},
            PropertyInfo{"role", PropertyKind::Opaque, "Role", "Role::MenuItem", {}, "What a reader is told this row is. The same distinction `checkSide` draws visually, and it has to be said separately because a screen reader announces the two differently: a **menu** holds commands — \"New branch, menu item\" — and a **select** holds values, where the reader also wants \"option, three of twelve\". A command that toggles is `MenuItemCheckbox`, which is what makes `selected` announce \"not checked\" instead of saying nothing.", false},
        },
        false,
        true,
        "bool menuItem(Ui& ui, const Interaction& input, std::string_view id, std::string_view label, const MenuItemOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "menuSeparator",
        "Overlays",
        "gbui/widgets/menu.hpp",
        "A rule between groups of items.",
        "The rows inside a popover.",
        "",
        {},
        false,
        false,
        "void menuSeparator(Ui& ui);",
    });
    out.push_back(ComponentInfo{
        "modal",
        "Overlays",
        "gbui/widgets/modal.hpp",
        "The caller supplies the position so dragging survives the tree being rebuilt, and gets it back updated.",
        "A modal dialog, with a backdrop and a header you can drag it by.",
        "ModalOptions",
        {
            PropertyInfo{"width", PropertyKind::Number, "float", "420.0f", {}, "", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"backdrop", PropertyKind::Bool, "bool", "true", {}, "Dims what is behind it. A modal without one is a floating panel.", false},
            PropertyInfo{"draggable", PropertyKind::Bool, "bool", "true", {}, "Lets the user drag it by its header, kept inside the window.", false},
            PropertyInfo{"icon", PropertyKind::Icon, "std::optional<Icon>", "", {}, "Beside the title. A dialog that asks something usually wants one; one that reports something usually does not.", true},
            PropertyInfo{"danger", PropertyKind::Bool, "bool", "false", {}, "The destructive kind. Recolours the title, and takes `AlertDialog` in the accessibility tree — which is what tells a reader they were interrupted rather than that they asked.", false},
        },
        false,
        true,
        "Modal modal(Ui& ui, const Interaction& input, std::string_view id, std::string_view title, Vec2 position, const ModalOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "modalActions",
        "Overlays",
        "gbui/widgets/modal.hpp",
        "The row of buttons at the foot of a dialog, pushed to the right.",
        "A modal dialog, with a backdrop and a header you can drag it by.",
        "",
        {},
        true,
        false,
        "Ui::Scope modalActions(Ui& ui);",
    });
    out.push_back(ComponentInfo{
        "popover",
        "Overlays",
        "gbui/widgets/popover.hpp",
        "Returns a scope, so its contents are written inside the braces like any other container.",
        "An empty floating surface the caller fills.",
        "PopoverOptions",
        {
            PropertyInfo{"minWidth", PropertyKind::Number, "float", "160.0f", {}, "", false},
            PropertyInfo{"maxWidth", PropertyKind::Number, "float", "420.0f", {}, "", false},
            PropertyInfo{"minHeight", PropertyKind::Number, "float", "kAuto", {}, "A ceiling on how tall it may grow. `kAuto` does *not* mean unbounded: it means **the room actually available on the side it lands on**, less the margin. A popup that would run past the bottom of the window stops at it and scrolls inside instead — which is what JetBrains' popups do, and the only behaviour that works when the anchor is near an edge. A number overrides that, and a smaller one wins.", false},
            PropertyInfo{"maxHeight", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"allowOverflow", PropertyKind::Bool, "bool", "false", {}, "Lets a popup grow past the window's edge, for a caller that would rather clip than scroll. Off, because the default should be reachable.", false},
            PropertyInfo{"scroll", PropertyKind::Enum, "ScrollAxis", "ScrollAxis::Vertical", {"None", "Vertical", "Horizontal"}, "Scrolls its own contents once `maxHeight` bites, rather than making the caller wrap everything in a scroll view. The axis is the caller's: `ScrollAxis::None` clips instead, which is what a popover that must never scroll wants. **`scrollState` has to be set too, or this does nothing.** Both of them together are what turns the ceiling into a scroll; an axis on its own leaves the box clipped, which looks like the popup simply lost its last rows. Three callers had set exactly this and only this, and the calendar opened near the bottom of a window was missing a week with no way to reach it. There is no default state to fall back on: state belongs to the application here as it does everywhere else.", false},
            PropertyInfo{"scrollState", PropertyKind::Opaque, "ScrollState*", "nullptr", {}, "Where the scroll position lives, when the popover scrolls its own contents. Null means it does not: the box is still bounded by `maxHeight` and still clips, but nothing moves. State belongs to the application here as it does everywhere else — a popover that kept its own offset would be a component with memory, and the tree is rebuilt every frame.", false},
            PropertyInfo{"matchAnchorWidth", PropertyKind::Bool, "bool", "false", {}, "Matches the anchor's width — what a select's list wants.", false},
            PropertyInfo{"padding", PropertyKind::Edges, "Edges", "Edges::all(6.0f)", {}, "", false},
            PropertyInfo{"gapBetweenItems", PropertyKind::Number, "float", "2.0f", {}, "Between the things *inside* the surface, as distinct from the padding around them. A menu wants a couple of pixels and a calendar wants none.", false},
            PropertyInfo{"role", PropertyKind::Opaque, "Role", "Role::None", {}, "What the surface is, to a reader who cannot see it float. `None` by default and on purpose: a popover is a *placement*, not a kind of thing, and the same box holds a menu, a list of values and a calendar. Whoever opened it knows which — `select` says `ListBox`, a context menu says `Menu` — and a default of \"menu\" would put that word in front of every date picker in the tree.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What it is called, when the role is worth announcing.", false},
        },
        true,
        true,
        "Ui::Scope popover(Ui& ui, const Interaction& input, std::string_view id, std::string_view anchorId, const PopoverOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "toast",
        "Overlays",
        "gbui/widgets/toast.hpp",
        "Draws the stack and advances its timers. `delta` is the frame's own seconds. Called once per outlet, near the end of the frame — it draws on the overlay layer, so where it sits in the tree decides nothing about where it appears, but building it last keeps it above anything that shares that layer. **It never takes the keyboard.** A message that stole focus would interrupt whatever the reader was typing, and the live region is what delivers it instead; only the × and the action are Tab stops, and only while they exist.",
        "Short-lived messages, stacked in a corner and gone on their own.",
        "ToastOptions",
        {
            PropertyInfo{"placement", PropertyKind::Enum, "ToastPlacement", "ToastPlacement::TopRight", {"TopLeft", "TopCenter", "TopRight", "BottomLeft", "BottomCenter", "BottomRight", "Anchored"}, "Which corner, edge or anchor the stack sits against. Which way it *grows* follows from it and is not a second decision.", false},
            PropertyInfo{"group", PropertyKind::Text, "std::string_view", "", {}, "Which entries this outlet shows. Empty shows the ungrouped ones.", false},
            PropertyInfo{"anchorId", PropertyKind::Text, "std::string_view", "", {}, "The tag `ToastPlacement::Anchored` measures against.", false},
            PropertyInfo{"bounds", PropertyKind::Opaque, "Rect", "", {}, "The rectangle the corners are measured from. Empty is the window. What lets a stack live inside a panel rather than over the whole screen — a docked tool reporting into its own pane rather than across the application it is embedded in.", false},
            PropertyInfo{"progress", PropertyKind::Bool, "bool", "true", {}, "A bar draining across the foot of each toast, showing the time left. Drawn only where there *is* a time left: a toast with no duration has nothing to count down and gets none.", false},
            PropertyInfo{"maxVisible", PropertyKind::Number, "std::size_t", "4", {}, "How many are on screen at once. The rest wait their turn, which is better than a stack taller than the window.", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "340.0f", {}, "", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "8.0f", {}, "Between two toasts in the stack.", false},
            PropertyInfo{"margin", PropertyKind::Number, "float", "16.0f", {}, "Between the stack and the edge it is anchored to.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "\"Notifications\"", {}, "What the region is called. A reader arriving at it out of context is otherwise told only that something changed.", false},
        },
        false,
        true,
        "ToastResult toast(Ui& ui, const Interaction& input, ToastState& state, float delta, const ToastOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "tooltip",
        "Overlays",
        "gbui/widgets/tooltip.hpp",
        "Draws nothing when the anchor is not hovered, so the call can sit unconditionally beside the control it describes.",
        "A tooltip, shown while its anchor is hovered.",
        "TooltipOptions",
        {
            PropertyInfo{"maxWidth", PropertyKind::Number, "float", "260.0f", {}, "", false},
            PropertyInfo{"delay", PropertyKind::Number, "float", "0.4f", {}, "How long the pointer must rest on the anchor first, in seconds. Without one, dragging the pointer across a toolbar flashes a tooltip for every control it passes over, which is noise rather than help. Needs an animator for the clock; with none, the tooltip shows at once and behaves as it always did.", false},
            PropertyInfo{"wrap", PropertyKind::Bool, "bool", "true", {}, "A long tooltip wraps rather than eliding — a description cut off at an ellipsis is worse than no description.", false},
        },
        false,
        true,
        "void tooltip(Ui& ui, const Interaction& input, std::string_view anchorId, std::string_view text, const TooltipOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "colorField",
        "Components",
        "gbui/widgets/colorPicker.hpp",
        "The same picker behind a swatch, opened in a popover. Two entry points rather than a flag, because they are two different components in every way that matters: the inline one is always open and owns its space, and this one is a control in a form that borrows space when asked. They share the state and the drawing, which is the part worth sharing. **The application owns whether it is open**, as it does for every other overlay here — `state.open` is a plain bool it can set, restore or ignore.",
        "A colour picker: a saturation/value square, a hue rail and an alpha rail.",
        "ColorFieldOptions",
        {
            PropertyInfo{"height", PropertyKind::Number, "float", "28.0f", {}, "The trigger's own size. The popover takes `width` from the picker options above, as everything else does.", false},
            PropertyInfo{"showHexOnTrigger", PropertyKind::Bool, "bool", "true", {}, "The hex code beside the swatch on the closed control. Off leaves the swatch alone, which is what a trigger in a tight toolbar wants.", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"dismissOnOutsideClick", PropertyKind::Bool, "bool", "true", {}, "A press anywhere else closes it. On, because a popup that outlives the attention that opened it is a popup the reader has to dismiss on purpose. Off keeps it open until the application says otherwise — which is what a picker being dragged from inside a dialog wants, where the click outside belongs to the dialog.", false},
            PropertyInfo{"dismissOnEscape", PropertyKind::Bool, "bool", "true", {}, "Escape closes it. Separate from the above: a caller may want the key without the click, and a modal that owns Escape may want neither.", false},
        },
        false,
        true,
        "ColorPickerResult colorField(Ui& ui, const Interaction& input, std::string_view id, ColorPickerState& state, const ColorFieldOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "colorPicker",
        "Components",
        "gbui/widgets/colorPicker.hpp",
        "Draws the picker and edits `state` in place. The square is built the way every picker on the web is: the pure hue behind, a white-to-transparent gradient across it and a transparent-to-black gradient down it. Two gradients over a fill is a two-dimensional ramp, and it costs three nodes rather than a per-pixel shader the painter does not have.",
        "A colour picker: a saturation/value square, a hue rail and an alpha rail.",
        "ColorPickerOptions",
        {
            PropertyInfo{"alpha", PropertyKind::Bool, "bool", "true", {}, "The alpha rail and the alpha in the readout. Off for a picker choosing a theme token, where a translucent colour is not a valid answer.", false},
            PropertyInfo{"hue", PropertyKind::Bool, "bool", "true", {}, "The hue rail. Off leaves a picker that only shades one hue, which is what a \"tint of the accent\" control wants.", false},
            PropertyInfo{"showHex", PropertyKind::Bool, "bool", "true", {}, "The `#rrggbb` readout under the rails.", false},
            PropertyInfo{"swatches", PropertyKind::Opaque, "std::vector<Color>", "", {}, "A row of fixed swatches under everything else. Empty draws none.", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "240.0f", {}, "", false},
            PropertyInfo{"squareHeight", PropertyKind::Number, "float", "150.0f", {}, "", false},
            PropertyInfo{"railHeight", PropertyKind::Number, "float", "14.0f", {}, "", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "10.0f", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What colour this is *of* — \"Accent\", \"Series 3\". The value announces itself as a hex code; only the caller knows what it colours.", false},
        },
        false,
        true,
        "ColorPickerResult colorPicker(Ui& ui, const Interaction& input, std::string_view id, ColorPickerState& state, const ColorPickerOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "dateField",
        "Components",
        "gbui/widgets/datePicker.hpp",
        "A calendar behind a field, opened in a popover. The counterpart to `colorField`, and the same split: the inline calendar owns its space, this one borrows it. They share the state and the grid.",
        "A calendar: a month of days, walked with the pointer or the keyboard.",
        "DateFieldOptions",
        {
            PropertyInfo{"placeholder", PropertyKind::Text, "std::string_view", "\"Select a date…\"", {}, "Shown when nothing is chosen. A field with no value has to say so. Falling back to today would be a lie — \"no date\" and \"today\" are different answers, and a form that silently fills one in gets the other submitted.", false},
            PropertyInfo{"pattern", PropertyKind::Text, "std::string_view", "\"dd/MM/yyyy\"", {}, "How the chosen date is written on the trigger. The same grammar `formatDate` takes, so the field reads the way the rest of the application writes dates.", false},
            PropertyInfo{"clearable", PropertyKind::Bool, "bool", "true", {}, "An x on the trigger that puts it back to nothing.", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "200.0f", {}, "", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "32.0f", {}, "", false},
            PropertyInfo{"dismissOnOutsideClick", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"dismissOnEscape", PropertyKind::Bool, "bool", "true", {}, "", false},
        },
        false,
        true,
        "DateFieldResult dateField(Ui& ui, const Interaction& input, std::string_view id, const Date& selected, DatePickerState& state, const DateFieldOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "datePicker",
        "Components",
        "gbui/widgets/datePicker.hpp",
        "Draws a month and reports the day chosen, if any. Stateless like everything else: `selected` comes in, the state carries only what is *being looked at*. The arrow keys move by a day and Page Up and Page Down by a month, both skipping nothing — a day outside the bounds is drawn and dimmed rather than hidden, because a calendar with holes in it is harder to read than one with unavailable days in it.",
        "A calendar: a month of days, walked with the pointer or the keyboard.",
        "DatePickerOptions",
        {
            PropertyInfo{"minimum", PropertyKind::Opaque, "Date", "", {}, "Nothing before this, or after that, can be chosen. An invalid date means no bound.", false},
            PropertyInfo{"maximum", PropertyKind::Opaque, "Date", "", {}, "", false},
            PropertyInfo{"today", PropertyKind::Opaque, "Date", "Date::today()", {}, "Drawn with a dot under it. Invalid draws none.", false},
            PropertyInfo{"locale", PropertyKind::Opaque, "CalendarLocale", "", {}, "The first day of the week, twelve month names and seven day initials. Answered rather than solved — see the note above `CalendarLocale`.", false},
            PropertyInfo{"showToday", PropertyKind::Bool, "bool", "true", {}, "Marks today with a dot. A dot rather than a ring, so it cannot be mistaken for the selection or for where the keyboard is.", false},
            PropertyInfo{"cellSize", PropertyKind::Number, "float", "30.0f", {}, "How big a day cell would like to be. A preference, not a promise: seven of these plus six gaps is a wide grid, and a calendar in a narrow column or a popover against the edge of a small window gets less than that. The cells shrink to fit rather than running out of the box, down to `minimumCellSize` and no further.", false},
            PropertyInfo{"minimumCellSize", PropertyKind::Number, "float", "22.0f", {}, "The floor the cells shrink to. Below about this a two-digit day stops being readable, and a grid that has to scroll sideways is worse than one that is merely tight.", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "2.0f", {}, "", false},
        },
        false,
        true,
        "DatePickerResult datePicker(Ui& ui, const Interaction& input, std::string_view id, const Date& selected, DatePickerState& state, const DatePickerOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "dateTimeField",
        "Components",
        "gbui/widgets/dateTimePicker.hpp",
        "A calendar and a clock behind one input. The pair belongs behind a single field far more often than beside each other on a page — a due date, a scheduled run, a deadline are one value, and asking for it in two controls invites half of it being filled in.",
        "A calendar and a clock, chosen together.",
        "DateTimeFieldOptions",
        {
            PropertyInfo{"placeholder", PropertyKind::Text, "std::string_view", "\"Select a date and time…\"", {}, "", false},
            PropertyInfo{"pattern", PropertyKind::Text, "std::string_view", "\"dd/MM/yyyy HH:mm\"", {}, "How the value is written on the trigger. One pattern for both halves, in the grammar `formatDate` and `formatTime` share — the date tokens are read by one and the clock tokens by the other, so `dd/MM/yyyy HH:mm` needs no separate spelling of where the date ends and the time begins.", false},
            PropertyInfo{"clearable", PropertyKind::Bool, "bool", "true", {}, "A small cross on the trigger once there is a value. Off where the field is required and empty is not an answer.", false},
            PropertyInfo{"disabled", PropertyKind::Bool, "bool", "false", {}, "", false},
            PropertyInfo{"width", PropertyKind::Number, "float", "240.0f", {}, "", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "0.0f", {}, "Zero takes the design's control height.", false},
            PropertyInfo{"dismissOnOutsideClick", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"dismissOnEscape", PropertyKind::Bool, "bool", "true", {}, "", false},
        },
        false,
        true,
        "DateTimeFieldResult dateTimeField(Ui& ui, const Interaction& input, std::string_view id, const DateTime& selected, DateTimeFieldState& state, const DateTimeFieldOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "dateTimePicker",
        "Components",
        "gbui/widgets/dateTimePicker.hpp",
        "The two pickers, sharing one value.",
        "A calendar and a clock, chosen together.",
        "DateTimePickerOptions",
        {
            PropertyInfo{"date", PropertyKind::Opaque, "DatePickerOptions", "", {}, "The two halves, each configured exactly as it would be on its own — bounds, locale, pattern. Passed through rather than restated, which is what keeps this one calendar and one clock instead of a third of each.", false},
            PropertyInfo{"time", PropertyKind::Opaque, "TimePickerOptions", "", {}, "As above.", false},
            PropertyInfo{"sideBySide", PropertyKind::Bool, "bool", "true", {}, "The clock beside the calendar, or under it. Beside is the default because a calendar is wider than it is tall and the pair balances.", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "16.0f", {}, "", false},
        },
        false,
        true,
        "DateTimePickerResult dateTimePicker(Ui& ui, const Interaction& input, std::string_view id, const DateTime& selected, DateTimePickerState& state, const DateTimePickerOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "richEditor",
        "Components",
        "gbui/widgets/richEditor.hpp",
        "Draws the editor and edits `document` in place. **What this is and is not.** It is a block editor: paragraphs, headings, lists, quotes and code blocks, with bold, italic, underline, strikethrough, inline code and links over ranges within a block. Typing, splitting a block with Return and merging with Backspace all work, and the toolbar is the caller's to compose. It is **not** finished, and the gaps are worth naming rather than discovering: no images — the painter cannot decode one yet; no undo; the caret moves by character and by block, not by *visual line*, so a block that wraps is edited by a caret that does not know where the lines are; and there is no nesting of lists. Each of those is a piece of work of its own, and three of them want engine features that do not exist.",
        "A rich text editor: blocks of text, marks over ranges, and a toolbar.",
        "RichEditorOptions",
        {
            PropertyInfo{"toolbar", PropertyKind::Opaque, "std::vector<ToolbarItem>", "", {}, "Empty takes `defaultToolbar()`. Anything else is exactly what is drawn, in that order — which is how a caller adds its own buttons, drops the ones it does not want, or reorders the rest.", false},
            PropertyInfo{"blockChoices", PropertyKind::Opaque, "std::vector<BlockChoice>", "", {}, "What the `BlockStyle` dropdown lists. Empty takes `defaultBlockChoices()`.", false},
            PropertyInfo{"showToolbar", PropertyKind::Bool, "bool", "true", {}, "Off leaves the keys and the document and nothing to click, which is what an editor whose buttons live somewhere else wants.", false},
            PropertyInfo{"placeholder", PropertyKind::Text, "std::string_view", "\"Write something…\"", {}, "", false},
            PropertyInfo{"minHeight", PropertyKind::Number, "float", "220.0f", {}, "", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "kAuto", {}, "", false},
            PropertyInfo{"grow", PropertyKind::Number, "float", "0.0f", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What is being written — \"Release notes\", \"Comment\". An editor with no name is the largest unlabelled thing on most screens.", false},
        },
        false,
        true,
        "RichEditorResult richEditor(Ui& ui, const Interaction& input, std::string_view id, RichDocument& document, RichEditorState& state, const RichEditorOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "timePicker",
        "Components",
        "gbui/widgets/timePicker.hpp",
        "Columns of hours, minutes and — when asked for — seconds, each scrolled to its own value. Columns rather than steppers because a time is picked far more often than it is nudged: \"quarter past two\" is two glances, and two number fields make it two edits. Each column keeps its selection in view, so opening the picker on 23:45 does not show midnight.",
        "A time of day, chosen from columns of hours and minutes.",
        "TimePickerOptions",
        {
            PropertyInfo{"use24Hour", PropertyKind::Bool, "bool", "true", {}, "Twelve-hour display with an AM/PM column. The *value* is always 0–23.", false},
            PropertyInfo{"showSeconds", PropertyKind::Bool, "bool", "false", {}, "A third column. Off, because most times are picked to the minute and a column nobody uses is a column in the way.", false},
            PropertyInfo{"minuteStep", PropertyKind::Number, "int", "1", {}, "Coarser columns: 5 or 15 for a picker nobody needs to the minute.", false},
            PropertyInfo{"secondStep", PropertyKind::Number, "int", "1", {}, "How the seconds column is filled: every second at 1, every fifteenth at 15. The step *for seconds* — not the second step.", false},
            PropertyInfo{"minimum", PropertyKind::Opaque, "Time", "0, 0, 0", {}, "Times outside these cannot be chosen. Leave both at midnight for none — a range of nothing is not a range anybody wants.", false},
            PropertyInfo{"maximum", PropertyKind::Opaque, "Time", "0, 0, 0", {}, "", false},
            PropertyInfo{"locale", PropertyKind::Opaque, "ClockLocale", "", {}, "The words: AM, PM, and the separator. The pattern decides the shape and this decides the words, which is how one formatter covers Brazilian, American and ISO without a locale database.", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "176.0f", {}, "", false},
            PropertyInfo{"columnWidth", PropertyKind::Number, "float", "58.0f", {}, "", false},
            PropertyInfo{"rowHeight", PropertyKind::Number, "float", "28.0f", {}, "", false},
        },
        false,
        true,
        "TimePickerResult timePicker(Ui& ui, const Interaction& input, std::string_view id, const Time& selected, TimePickerState& state, const TimePickerOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "barChart",
        "Charts",
        "gbui/widgets/chart.hpp",
        "Bars, columns or lollipops over a shared scale. One function rather than four because they differ only in how a value is turned into a rectangle: transposing the axes gives a bar chart from a column chart, stacking changes where each bar starts, and a lollipop is a bar drawn as a stem. Splitting them would mean four copies of the scale, the axis, the grid and the hit testing.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "BarChartOptions",
        {
            PropertyInfo{"scale", PropertyKind::Opaque, "Scale", "0.0, 0.0", {}, "Fixed bounds. Left as they are, the chart takes them from the data.", false},
            PropertyInfo{"autoScale", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"tickCount", PropertyKind::Number, "std::optional<int>", "", {}, "Each of these falls back to `Design::chart` when unset.", true},
            PropertyInfo{"axisWidth", PropertyKind::Number, "std::optional<float>", "", {}, "Room for the value labels. Zero draws none.", true},
            PropertyInfo{"height", PropertyKind::Number, "float", "160.0f", {}, "", false},
            PropertyInfo{"grid", PropertyKind::Bool, "std::optional<bool>", "", {}, "", true},
            PropertyInfo{"hover", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"valueFormat", PropertyKind::Text, "std::string_view", "\"%.0f\"", {}, "`std::snprintf` style, for the value labels and readouts.", false},
            PropertyInfo{"horizontal", PropertyKind::Bool, "bool", "false", {}, "Bars run left to right from a category axis down the side, instead of bottom to top from one along the bottom. Long category names are the usual reason: horizontal gives them room to be read.", false},
            PropertyInfo{"grouping", PropertyKind::Enum, "BarGrouping", "BarGrouping::Grouped", {"Grouped", "Stacked"}, "How several series share a category: side by side, or on top of each other.", false},
            PropertyInfo{"shape", PropertyKind::Enum, "BarShape", "BarShape::Bar", {"Bar", "Lollipop"}, "A filled rectangle from the baseline, or a stem with a dot at the value.", false},
            PropertyInfo{"categoryPadding", PropertyKind::Number, "float", "0.28f", {}, "Share of each category's slot left as gap, 0 to 0.9.", false},
            PropertyInfo{"seriesGap", PropertyKind::Number, "float", "2.0f", {}, "Between the bars of different series in one category, in pixels.", false},
            PropertyInfo{"radius", PropertyKind::Number, "float", "2.0f", {}, "", false},
            PropertyInfo{"categories", PropertyKind::Opaque, "std::vector<std::string>", "", {}, "Names along the category axis. Fewer than there are values leaves the rest unlabelled rather than renumbering them.", false},
            PropertyInfo{"categoryAxis", PropertyKind::Number, "float", "22.0f", {}, "Room for the category names. Zero draws none.", false},
            PropertyInfo{"tooltip", PropertyKind::Opaque, "ChartTooltip", "", {}, "", false},
            PropertyInfo{"link", PropertyKind::Opaque, "ChartLink*", "nullptr", {}, "Ties this chart's readout to the other charts sharing the same `ChartLink`, so pointing at a sample in one points at it in all of them. Null leaves the chart on its own.", false},
            PropertyInfo{"legend", PropertyKind::Opaque, "ChartLegend", "", {}, "The key drawn under the chart, and the focus a click on it gives.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the chart is of — \"Revenue by month\". A chart is the one thing on a screen a reader gets nothing from: the shapes carry all of it, and the numbers behind them are the caller's. A name and a `Figure` role are the least that makes it navigable, and they are not the whole answer — see the note in `docs/reference/accessibility`.", false},
        },
        false,
        true,
        "ChartResult barChart(Ui& ui, const Interaction& input, std::string_view id, const std::vector<Series>& series, const BarChartOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "barChart",
        "Charts",
        "gbui/widgets/chart.hpp",
        "The same bars, showing only `view` of the categories and letting the reader move it. The view is the caller's, like everything else here, and sharing one is the point: a volume chart handed the same `ChartView` as the price chart above it zooms when that one is swept, because they are looking at the same window of the same data rather than each keeping their own idea of it.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "BarChartOptions",
        {
            PropertyInfo{"scale", PropertyKind::Opaque, "Scale", "0.0, 0.0", {}, "Fixed bounds. Left as they are, the chart takes them from the data.", false},
            PropertyInfo{"autoScale", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"tickCount", PropertyKind::Number, "std::optional<int>", "", {}, "Each of these falls back to `Design::chart` when unset.", true},
            PropertyInfo{"axisWidth", PropertyKind::Number, "std::optional<float>", "", {}, "Room for the value labels. Zero draws none.", true},
            PropertyInfo{"height", PropertyKind::Number, "float", "160.0f", {}, "", false},
            PropertyInfo{"grid", PropertyKind::Bool, "std::optional<bool>", "", {}, "", true},
            PropertyInfo{"hover", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"valueFormat", PropertyKind::Text, "std::string_view", "\"%.0f\"", {}, "`std::snprintf` style, for the value labels and readouts.", false},
            PropertyInfo{"horizontal", PropertyKind::Bool, "bool", "false", {}, "Bars run left to right from a category axis down the side, instead of bottom to top from one along the bottom. Long category names are the usual reason: horizontal gives them room to be read.", false},
            PropertyInfo{"grouping", PropertyKind::Enum, "BarGrouping", "BarGrouping::Grouped", {"Grouped", "Stacked"}, "How several series share a category: side by side, or on top of each other.", false},
            PropertyInfo{"shape", PropertyKind::Enum, "BarShape", "BarShape::Bar", {"Bar", "Lollipop"}, "A filled rectangle from the baseline, or a stem with a dot at the value.", false},
            PropertyInfo{"categoryPadding", PropertyKind::Number, "float", "0.28f", {}, "Share of each category's slot left as gap, 0 to 0.9.", false},
            PropertyInfo{"seriesGap", PropertyKind::Number, "float", "2.0f", {}, "Between the bars of different series in one category, in pixels.", false},
            PropertyInfo{"radius", PropertyKind::Number, "float", "2.0f", {}, "", false},
            PropertyInfo{"categories", PropertyKind::Opaque, "std::vector<std::string>", "", {}, "Names along the category axis. Fewer than there are values leaves the rest unlabelled rather than renumbering them.", false},
            PropertyInfo{"categoryAxis", PropertyKind::Number, "float", "22.0f", {}, "Room for the category names. Zero draws none.", false},
            PropertyInfo{"tooltip", PropertyKind::Opaque, "ChartTooltip", "", {}, "", false},
            PropertyInfo{"link", PropertyKind::Opaque, "ChartLink*", "nullptr", {}, "Ties this chart's readout to the other charts sharing the same `ChartLink`, so pointing at a sample in one points at it in all of them. Null leaves the chart on its own.", false},
            PropertyInfo{"legend", PropertyKind::Opaque, "ChartLegend", "", {}, "The key drawn under the chart, and the focus a click on it gives.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the chart is of — \"Revenue by month\". A chart is the one thing on a screen a reader gets nothing from: the shapes carry all of it, and the numbers behind them are the caller's. A name and a `Figure` role are the least that makes it navigable, and they are not the whole answer — see the note in `docs/reference/accessibility`.", false},
        },
        false,
        true,
        "ChartResult barChart(Ui& ui, const Interaction& input, std::string_view id, const std::vector<Series>& series, ChartView& view, const BarChartOptions& options = {}, const ChartZoom& zoom = {});",
    });
    out.push_back(ComponentInfo{
        "candlestickChart",
        "Charts",
        "gbui/widgets/chart.hpp",
        "Open, high, low and close per period. The one chart here whose scale deliberately does **not** reach zero. Every other chart in this file widens its range to include the baseline, because a bar that does not start at zero misstates the ratio between two bars. A price has no such baseline — forcing one on a stock that trades between 180 and 190 squeezes the entire year into the top five percent of the plot and hides the only thing the chart was drawn to show.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "CandlestickOptions",
        {
            PropertyInfo{"scale", PropertyKind::Opaque, "Scale", "0.0, 0.0", {}, "Fixed bounds. Left as they are, the chart takes them from the data.", false},
            PropertyInfo{"autoScale", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"tickCount", PropertyKind::Number, "std::optional<int>", "", {}, "Each of these falls back to `Design::chart` when unset.", true},
            PropertyInfo{"axisWidth", PropertyKind::Number, "std::optional<float>", "", {}, "Room for the value labels. Zero draws none.", true},
            PropertyInfo{"height", PropertyKind::Number, "float", "160.0f", {}, "", false},
            PropertyInfo{"grid", PropertyKind::Bool, "std::optional<bool>", "", {}, "", true},
            PropertyInfo{"hover", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"valueFormat", PropertyKind::Text, "std::string_view", "\"%.0f\"", {}, "`std::snprintf` style, for the value labels and readouts.", false},
            PropertyInfo{"categoryPadding", PropertyKind::Number, "float", "0.3f", {}, "Share of each period's slot left as gap, 0 to 0.9.", false},
            PropertyInfo{"maxBodyWidth", PropertyKind::Number, "float", "14.0f", {}, "A ceiling on the body, in pixels, whatever the slot allows. A candle is a *mark*, not a bar: zooming in should make it taller and put more air around it, not fatten it. Without the ceiling a chart zoomed to a dozen periods draws them as forty-pixel bricks, which is the width of a bar chart's column and reads as one. The default barely bites at a full view — forty-odd candles across a card land near it anyway — and does all its work once the reader has swept a range.", false},
            PropertyInfo{"categories", PropertyKind::Opaque, "std::vector<std::string>", "", {}, "", false},
            PropertyInfo{"categoryAxis", PropertyKind::Number, "float", "22.0f", {}, "Room for the category names. Zero draws none.", false},
            PropertyInfo{"rising", PropertyKind::Token, "std::optional<Token>", "", {}, "Unset takes the theme's added/removed colours, which are already the two a reader of this application associates with up and down.", true},
            PropertyInfo{"falling", PropertyKind::Token, "std::optional<Token>", "", {}, "As above, for the other direction.", true},
            PropertyInfo{"hollowRising", PropertyKind::Bool, "bool", "false", {}, "Draw rising candles as an outline instead of a solid body. The other common convention, and worth having: on a dense chart the hollow bodies recede and the falling ones stand out, which is what a reader watching for drops actually wants.", false},
            PropertyInfo{"lineWidth", PropertyKind::Number, "float", "1.0f", {}, "Width of the wick, and of a hollow body's outline.", false},
            PropertyInfo{"tooltip", PropertyKind::Opaque, "ChartTooltip", "", {}, "", false},
            PropertyInfo{"link", PropertyKind::Opaque, "ChartLink*", "nullptr", {}, "Ties this chart's readout to the other charts sharing the same `ChartLink`, so pointing at a sample in one points at it in all of them. Null leaves the chart on its own.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the chart is of — \"Revenue by month\". A chart is the one thing on a screen a reader gets nothing from: the shapes carry all of it, and the numbers behind them are the caller's. A name and a `Figure` role are the least that makes it navigable, and they are not the whole answer — see the note in `docs/reference/accessibility`.", false},
        },
        false,
        true,
        "ChartResult candlestickChart(Ui& ui, const Interaction& input, std::string_view id, const std::vector<Candle>& candles, const CandlestickOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "candlestickChart",
        "Charts",
        "gbui/widgets/chart.hpp",
        "The same candles, showing only `view` of them — the overload a price chart and its volume share, so sweeping either zooms both.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "CandlestickOptions",
        {
            PropertyInfo{"scale", PropertyKind::Opaque, "Scale", "0.0, 0.0", {}, "Fixed bounds. Left as they are, the chart takes them from the data.", false},
            PropertyInfo{"autoScale", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"tickCount", PropertyKind::Number, "std::optional<int>", "", {}, "Each of these falls back to `Design::chart` when unset.", true},
            PropertyInfo{"axisWidth", PropertyKind::Number, "std::optional<float>", "", {}, "Room for the value labels. Zero draws none.", true},
            PropertyInfo{"height", PropertyKind::Number, "float", "160.0f", {}, "", false},
            PropertyInfo{"grid", PropertyKind::Bool, "std::optional<bool>", "", {}, "", true},
            PropertyInfo{"hover", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"valueFormat", PropertyKind::Text, "std::string_view", "\"%.0f\"", {}, "`std::snprintf` style, for the value labels and readouts.", false},
            PropertyInfo{"categoryPadding", PropertyKind::Number, "float", "0.3f", {}, "Share of each period's slot left as gap, 0 to 0.9.", false},
            PropertyInfo{"maxBodyWidth", PropertyKind::Number, "float", "14.0f", {}, "A ceiling on the body, in pixels, whatever the slot allows. A candle is a *mark*, not a bar: zooming in should make it taller and put more air around it, not fatten it. Without the ceiling a chart zoomed to a dozen periods draws them as forty-pixel bricks, which is the width of a bar chart's column and reads as one. The default barely bites at a full view — forty-odd candles across a card land near it anyway — and does all its work once the reader has swept a range.", false},
            PropertyInfo{"categories", PropertyKind::Opaque, "std::vector<std::string>", "", {}, "", false},
            PropertyInfo{"categoryAxis", PropertyKind::Number, "float", "22.0f", {}, "Room for the category names. Zero draws none.", false},
            PropertyInfo{"rising", PropertyKind::Token, "std::optional<Token>", "", {}, "Unset takes the theme's added/removed colours, which are already the two a reader of this application associates with up and down.", true},
            PropertyInfo{"falling", PropertyKind::Token, "std::optional<Token>", "", {}, "As above, for the other direction.", true},
            PropertyInfo{"hollowRising", PropertyKind::Bool, "bool", "false", {}, "Draw rising candles as an outline instead of a solid body. The other common convention, and worth having: on a dense chart the hollow bodies recede and the falling ones stand out, which is what a reader watching for drops actually wants.", false},
            PropertyInfo{"lineWidth", PropertyKind::Number, "float", "1.0f", {}, "Width of the wick, and of a hollow body's outline.", false},
            PropertyInfo{"tooltip", PropertyKind::Opaque, "ChartTooltip", "", {}, "", false},
            PropertyInfo{"link", PropertyKind::Opaque, "ChartLink*", "nullptr", {}, "Ties this chart's readout to the other charts sharing the same `ChartLink`, so pointing at a sample in one points at it in all of them. Null leaves the chart on its own.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the chart is of — \"Revenue by month\". A chart is the one thing on a screen a reader gets nothing from: the shapes carry all of it, and the numbers behind them are the caller's. A name and a `Figure` role are the least that makes it navigable, and they are not the whole answer — see the note in `docs/reference/accessibility`.", false},
        },
        false,
        true,
        "ChartResult candlestickChart(Ui& ui, const Interaction& input, std::string_view id, const std::vector<Candle>& candles, ChartView& view, const CandlestickOptions& options = {}, const ChartZoom& zoom = {});",
    });
    out.push_back(ComponentInfo{
        "chartBrush",
        "Charts",
        "gbui/widgets/chart.hpp",
        "A strip of the whole series with the current view as a window over it. The part an ApexCharts-style brush actually earns its keep with is not the zooming — a drag inside the plot does that, and so does the wheel — but the *context*: once zoomed in, a reader has no way to tell where they are or how much they cannot see. The strip answers both without them having to zoom back out, which is also why it is optional. Returns true on any frame the view moved.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "BrushOptions",
        {
            PropertyInfo{"height", PropertyKind::Number, "float", "46.0f", {}, "", false},
            PropertyInfo{"axisWidth", PropertyKind::Number, "float", "0.0f", {}, "Room down the left, so the strip lines up with the chart above it.", false},
            PropertyInfo{"handleWidth", PropertyKind::Number, "float", "8.0f", {}, "Handle width, and the smallest grabbable window.", false},
            PropertyInfo{"dragSelects", PropertyKind::Bool, "bool", "true", {}, "Dragging the strip's empty part draws a new window instead of moving the existing one — which is how a reader selects a range they can see but are not in.", false},
        },
        false,
        true,
        "bool chartBrush(Ui& ui, const Interaction& input, std::string_view id, const std::vector<Series>& series, ChartView& view, const BrushOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "chartToolbar",
        "Charts",
        "gbui/widgets/chart.hpp",
        "The buttons ApexCharts puts in the corner of a chart: zoom in, zoom out, and put it back. A row of ordinary buttons over the caller's own `ChartView`, and nothing else — which is why it is a component rather than an option on the chart. It has no idea which chart it belongs to, so one row can drive a group of them the same way one view already zooms a price and its volume together, and a caller who wants it somewhere other than the top right corner simply puts it there. The gestures are the primary way in and this is the secondary one, for the same reason a map has both: a sweep says *which range*, and these say *a bit more* — and only one of the two can be pressed by someone who has already lost the thread of where they are. Returns true on any frame the view moved.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "ChartToolbarOptions",
        {
            PropertyInfo{"zoomIn", PropertyKind::Bool, "bool", "true", {}, "Which of the three buttons the strip carries. All of them by default; drop the ones a chart has no room for.", false},
            PropertyInfo{"zoomOut", PropertyKind::Bool, "bool", "true", {}, "As above.", false},
            PropertyInfo{"reset", PropertyKind::Bool, "bool", "true", {}, "Puts the whole series back. Disabled while it already is whole, because a button that does nothing is worse than one that is not there — the reader presses it and learns nothing about why.", false},
            PropertyInfo{"step", PropertyKind::Number, "double", "0.4", {}, "How much of the window a press takes off, as a fraction of it. 0.4 rather than a half: two presses should not lose 75% of the data, and a reader who wants that has the sweep, which says exactly what to keep.", false},
            PropertyInfo{"minSpan", PropertyKind::Number, "double", "0.02", {}, "The narrowest the view may get, as a fraction of the whole. The same floor `BrushOptions` uses, so the two controls agree about how far in a reader is allowed to go.", false},
            PropertyInfo{"variant", PropertyKind::Enum, "ButtonVariant", "ButtonVariant::Secondary", {"Primary", "Secondary", "Ghost", "Danger"}, "How the buttons are drawn. `Secondary` by default, which gives them a surface and an outline: three bare glyphs floating in a card header read as marks rather than as controls, and a reader has to try one to find out it can be pressed. `Ghost` is the bare version, for a toolbar that sits on top of the plot where a surface would be in the way.", false},
            PropertyInfo{"height", PropertyKind::Number, "float", "30.0f", {}, "The buttons are square, so this is their size.", false},
            PropertyInfo{"iconSize", PropertyKind::Number, "float", "17.0f", {}, "Bigger than a button with a label would use: the glyph is the whole button here, and at fourteen pixels in a thirty-pixel square it reads as a mark stranded in the middle of a box.", false},
        },
        false,
        true,
        "bool chartToolbar(Ui& ui, const Interaction& input, std::string_view id, ChartView& view, const ChartToolbarOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "donutChart",
        "Charts",
        "gbui/widgets/chart.hpp",
        "A donut, or a pie when `thickness` is 1. Proof that the canvas node carries more than polylines: each wedge is two arcs and two lines, and an arc is cubics. Nothing here is a special case in the painter — it is the same vector layer the icons ride on.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "DonutOptions",
        {
            PropertyInfo{"size", PropertyKind::Number, "float", "150.0f", {}, "", false},
            PropertyInfo{"thickness", PropertyKind::Number, "std::optional<float>", "", {}, "How thick the ring is, as a share of the radius. 1 is a full pie. Unset takes the design's.", true},
            PropertyInfo{"padAngle", PropertyKind::Number, "std::optional<float>", "", {}, "A gap between wedges, in degrees, so neighbouring colours do not merge.", true},
            PropertyInfo{"startAngle", PropertyKind::Number, "float", "-90.0f", {}, "Where the first wedge starts. -90 puts it at twelve o'clock, which is where every reader expects a donut to begin.", false},
            PropertyInfo{"hoverGrow", PropertyKind::Number, "float", "4.0f", {}, "The hovered wedge grows outward by this much.", false},
            PropertyInfo{"focusGrow", PropertyKind::Number, "float", "10.0f", {}, "How much further a *focused* wedge pulls out of the ring.", false},
            PropertyInfo{"dimAlpha", PropertyKind::Number, "float", "0.3f", {}, "What the wedges that were not singled out fade to.", false},
            PropertyInfo{"legend", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"legendMaxHeight", PropertyKind::Number, "float", "kAuto", {}, "A ceiling on the legend before it scrolls, so a chart with forty contributors does not grow taller than the ring beside it. `kAuto` matches the donut.", false},
            PropertyInfo{"tooltip", PropertyKind::Opaque, "ChartTooltip", "", {}, "The readout beside the hovered wedge. A donut's legend already names every slice, so this one is about the *number* — which the legend has no room for once there are more than a handful.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the chart is of — \"Revenue by month\". A chart is the one thing on a screen a reader gets nothing from: the shapes carry all of it, and the numbers behind them are the caller's. A name and a `Figure` role are the least that makes it navigable, and they are not the whole answer — see the note in `docs/reference/accessibility`.", false},
        },
        false,
        true,
        "DonutResult donutChart(Ui& ui, const Interaction& input, std::string_view id, const std::vector<Slice>& slices, DonutState& state, const DonutOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "heatmap",
        "Charts",
        "gbui/widgets/chart.hpp",
        "A grid of cells shaded by value. `values` is row-major and may be ragged — a short row simply has fewer cells, which is what a calendar's last week is. The colour is one token at varying strength rather than a rainbow. A multi-hue scale looks richer and reads worse: hue carries no order, so a reader has to consult the legend for every cell, while \"more of the same colour\" needs no legend at all.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "HeatmapOptions",
        {
            PropertyInfo{"rows", PropertyKind::Opaque, "std::vector<std::string>", "", {}, "Names down the side and along the top. Fewer than there are rows or columns leaves the rest unlabelled rather than renumbering them.", false},
            PropertyInfo{"columns", PropertyKind::Opaque, "std::vector<std::string>", "", {}, "As above, along the top.", false},
            PropertyInfo{"scale", PropertyKind::Opaque, "Scale", "0.0, 0.0", {}, "", false},
            PropertyInfo{"autoScale", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"steps", PropertyKind::Number, "int", "5", {}, "Quantise the colour into this many steps; zero is a continuous ramp. Five by default, and steps rather than a ramp because a reader compares cells by *matching* them, not by judging absolute lightness. Two cells one step apart are visibly different; two cells 3% apart on a continuous ramp are the same colour to anyone reading a grid at a glance.", false},
            PropertyInfo{"color", PropertyKind::Token, "std::optional<Token>", "", {}, "The hue the scale runs to. Unset takes the design's first chart colour, so the grid re-themes with everything else.", true},
            PropertyInfo{"cellSize", PropertyKind::Number, "float", "0.0f", {}, "Zero lets the cells share out the width.", false},
            PropertyInfo{"gap", PropertyKind::Number, "float", "3.0f", {}, "", false},
            PropertyInfo{"radius", PropertyKind::Number, "float", "2.0f", {}, "", false},
            PropertyInfo{"rowLabels", PropertyKind::Number, "float", "34.0f", {}, "Room for the labels. Zero draws none.", false},
            PropertyInfo{"columnLabels", PropertyKind::Number, "float", "16.0f", {}, "The same, along the top.", false},
            PropertyInfo{"hover", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"valueFormat", PropertyKind::Text, "std::string_view", "\"%.0f\"", {}, "", false},
            PropertyInfo{"tooltip", PropertyKind::Opaque, "ChartTooltip", "", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the chart is of — \"Revenue by month\". A chart is the one thing on a screen a reader gets nothing from: the shapes carry all of it, and the numbers behind them are the caller's. A name and a `Figure` role are the least that makes it navigable, and they are not the whole answer — see the note in `docs/reference/accessibility`.", false},
        },
        false,
        true,
        "HeatmapResult heatmap(Ui& ui, const Interaction& input, std::string_view id, const std::vector<std::vector<double>>& values, const HeatmapOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "lineChart",
        "Charts",
        "gbui/widgets/chart.hpp",
        "Draws one or more series over a shared scale. The geometry is built here and handed to `Ui::draw`, so the whole chart is one node plus its labels rather than a widget per mark. Hit testing is on the *index* — the pointer's x is turned back into a sample number — which is what lets a tooltip name the right point instead of the nearest pixel.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "ChartOptions",
        {
            PropertyInfo{"scale", PropertyKind::Opaque, "Scale", "0.0, 0.0", {}, "Fixed bounds. Left as they are, the chart takes them from the data — which is what a live readout wants and what a comparison does not.", false},
            PropertyInfo{"autoScale", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"tickCount", PropertyKind::Number, "std::optional<int>", "", {}, "Each of these falls back to `Design::chart` when it is not set, so a dashboard configures its charts once instead of per call site.", true},
            PropertyInfo{"axisWidth", PropertyKind::Number, "std::optional<float>", "", {}, "Room for the value labels down the left. Zero draws none.", true},
            PropertyInfo{"height", PropertyKind::Number, "float", "120.0f", {}, "", false},
            PropertyInfo{"grid", PropertyKind::Bool, "std::optional<bool>", "", {}, "", true},
            PropertyInfo{"hover", PropertyKind::Bool, "bool", "true", {}, "A crosshair and a readout following the pointer.", false},
            PropertyInfo{"valueFormat", PropertyKind::Text, "std::string_view", "\"%.0f\"", {}, "Printed beside a hovered value; \"%.0f\" style, as `std::snprintf` takes.", false},
            PropertyInfo{"categories", PropertyKind::Opaque, "std::vector<std::string>", "", {}, "Names for the samples, used as the readout's heading.", false},
            PropertyInfo{"tooltip", PropertyKind::Opaque, "ChartTooltip", "", {}, "", false},
            PropertyInfo{"link", PropertyKind::Opaque, "ChartLink*", "nullptr", {}, "Ties this chart's readout to the other charts sharing the same `ChartLink`, so pointing at a sample in one points at it in all of them. Null leaves the chart on its own.", false},
            PropertyInfo{"legend", PropertyKind::Opaque, "ChartLegend", "", {}, "The key drawn under the chart, and the focus a click on it gives.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the chart is of — \"Revenue by month\". A chart is the one thing on a screen a reader gets nothing from: the shapes carry all of it, and the numbers behind them are the caller's. A name and a `Figure` role are the least that makes it navigable, and they are not the whole answer — see the note in `docs/reference/accessibility`.", false},
        },
        false,
        true,
        "ChartResult lineChart(Ui& ui, const Interaction& input, std::string_view id, const std::vector<Series>& series, const ChartOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "lineChart",
        "Charts",
        "gbui/widgets/chart.hpp",
        "The same chart, showing only `view` of the data and letting the reader move it. The view is the caller's, like every other piece of state here — which is what lets two charts share one, so panning a line chart pans the bars under it.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "ChartOptions",
        {
            PropertyInfo{"scale", PropertyKind::Opaque, "Scale", "0.0, 0.0", {}, "Fixed bounds. Left as they are, the chart takes them from the data — which is what a live readout wants and what a comparison does not.", false},
            PropertyInfo{"autoScale", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"tickCount", PropertyKind::Number, "std::optional<int>", "", {}, "Each of these falls back to `Design::chart` when it is not set, so a dashboard configures its charts once instead of per call site.", true},
            PropertyInfo{"axisWidth", PropertyKind::Number, "std::optional<float>", "", {}, "Room for the value labels down the left. Zero draws none.", true},
            PropertyInfo{"height", PropertyKind::Number, "float", "120.0f", {}, "", false},
            PropertyInfo{"grid", PropertyKind::Bool, "std::optional<bool>", "", {}, "", true},
            PropertyInfo{"hover", PropertyKind::Bool, "bool", "true", {}, "A crosshair and a readout following the pointer.", false},
            PropertyInfo{"valueFormat", PropertyKind::Text, "std::string_view", "\"%.0f\"", {}, "Printed beside a hovered value; \"%.0f\" style, as `std::snprintf` takes.", false},
            PropertyInfo{"categories", PropertyKind::Opaque, "std::vector<std::string>", "", {}, "Names for the samples, used as the readout's heading.", false},
            PropertyInfo{"tooltip", PropertyKind::Opaque, "ChartTooltip", "", {}, "", false},
            PropertyInfo{"link", PropertyKind::Opaque, "ChartLink*", "nullptr", {}, "Ties this chart's readout to the other charts sharing the same `ChartLink`, so pointing at a sample in one points at it in all of them. Null leaves the chart on its own.", false},
            PropertyInfo{"legend", PropertyKind::Opaque, "ChartLegend", "", {}, "The key drawn under the chart, and the focus a click on it gives.", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the chart is of — \"Revenue by month\". A chart is the one thing on a screen a reader gets nothing from: the shapes carry all of it, and the numbers behind them are the caller's. A name and a `Figure` role are the least that makes it navigable, and they are not the whole answer — see the note in `docs/reference/accessibility`.", false},
        },
        false,
        true,
        "ChartResult lineChart(Ui& ui, const Interaction& input, std::string_view id, const std::vector<Series>& series, ChartView& view, const ChartOptions& options = {}, const ChartZoom& zoom = {});",
    });
    out.push_back(ComponentInfo{
        "radarChart",
        "Charts",
        "gbui/widgets/chart.hpp",
        "One value per axis, per series, on spokes around a common centre. The chart for a *profile* — a handful of measures that belong together and are compared as a shape rather than read off one at a time: a skills assessment, a wine's tasting notes, one machine's five utilisation figures against another's. Everything it is good at falls apart past about eight axes, where the polygon stops being a shape and becomes a scribble. `series` is the same `Series` every other chart here takes; each value is a spoke, in order, starting at twelve o'clock and going clockwise. Hit testing is by *angle*, so a reader pointing anywhere along a spoke means that axis — the same rule the bars use for a category, for the same reason.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "RadarOptions",
        {
            PropertyInfo{"scale", PropertyKind::Opaque, "Scale", "0.0, 0.0", {}, "Fixed bounds. Left alone the chart takes them from the data — and always from zero, whatever the data says. A radar is read as an *area*, and area from a non-zero baseline is the same lie a truncated bar tells, told about five axes at once: two profiles a hair apart become one enormous and one tiny.", false},
            PropertyInfo{"autoScale", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"tickCount", PropertyKind::Number, "std::optional<int>", "", {}, "", true},
            PropertyInfo{"categories", PropertyKind::Opaque, "std::vector<std::string>", "", {}, "One name per axis, drawn outside its spoke. Fewer than there are values leaves the rest unlabelled rather than renumbering them.", false},
            PropertyInfo{"size", PropertyKind::Number, "float", "240.0f", {}, "The whole chart's box, labels included.", false},
            PropertyInfo{"grid", PropertyKind::Enum, "RadarGrid", "RadarGrid::Web", {"Web", "Polygon", "None"}, "What is drawn behind the data: a mesh, shaded bands, or nothing.", false},
            PropertyInfo{"labelRoom", PropertyKind::Number, "float", "52.0f", {}, "Room between the outermost ring and the edge, for the names. Zero draws none and gives the web the whole box.", false},
            PropertyInfo{"fillAlpha", PropertyKind::Number, "float", "0.1f", {}, "Shading inside a series' polygon, when the series carries no `fillAlpha` of its own. Filled by default, where a line chart is not. A line is read along, and shading under two of them buries the lower one; a radar is read as a shape, and an unfilled one is a wire outline the eye has to close for itself. Lower than the 0.2 the web charting libraries settle on, and deliberately: the painter composites in linear light, where the same number covers roughly twice as much as it does in a browser blending sRGB directly.", false},
            PropertyInfo{"markerRadius", PropertyKind::Number, "float", "3.0f", {}, "A dot at each vertex. Zero draws none.", false},
            PropertyInfo{"hover", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"valueFormat", PropertyKind::Text, "std::string_view", "\"%.0f\"", {}, "Printed in the readout; \"%.0f\" style, as `std::snprintf` takes.", false},
            PropertyInfo{"tooltip", PropertyKind::Opaque, "ChartTooltip", "", {}, "", false},
            PropertyInfo{"legend", PropertyKind::Opaque, "ChartLegend", "", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the chart is of — \"Revenue by month\". A chart is the one thing on a screen a reader gets nothing from: the shapes carry all of it, and the numbers behind them are the caller's. A name and a `Figure` role are the least that makes it navigable, and they are not the whole answer — see the note in `docs/reference/accessibility`.", false},
        },
        false,
        true,
        "ChartResult radarChart(Ui& ui, const Interaction& input, std::string_view id, const std::vector<Series>& series, const RadarOptions& options = {});",
    });
    out.push_back(ComponentInfo{
        "scatterChart",
        "Charts",
        "gbui/widgets/chart.hpp",
        "Points on two continuous scales — a scatter, or a bubble chart when the points carry weights. The first chart here with a real **x scale**. Every other one indexes its samples along the bottom: sample 0 at the left, sample n at the right, evenly spaced whatever their x values are. That is right for a series over time and wrong for a correlation, where the whole question is where the points sit against each other on both axes. Hit testing is by distance to the nearest dot rather than by column, because a scatter has no columns — two points can share an x and mean different things.",
        "Charts: a scale, its ticks, and a line or area drawn from data.",
        "ScatterOptions",
        {
            PropertyInfo{"xScale", PropertyKind::Opaque, "Scale", "0.0, 0.0", {}, "Fixed bounds for each axis. Left alone, both come from the data.", false},
            PropertyInfo{"yScale", PropertyKind::Opaque, "Scale", "0.0, 0.0", {}, "As above, for the other one.", false},
            PropertyInfo{"autoScale", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"tickCount", PropertyKind::Number, "std::optional<int>", "", {}, "", true},
            PropertyInfo{"axisWidth", PropertyKind::Number, "std::optional<float>", "", {}, "", true},
            PropertyInfo{"height", PropertyKind::Number, "float", "220.0f", {}, "", false},
            PropertyInfo{"grid", PropertyKind::Bool, "std::optional<bool>", "", {}, "", true},
            PropertyInfo{"hover", PropertyKind::Bool, "bool", "true", {}, "", false},
            PropertyInfo{"valueFormat", PropertyKind::Text, "std::string_view", "\"%.0f\"", {}, "", false},
            PropertyInfo{"xFormat", PropertyKind::Text, "std::string_view", "\"%.0f\"", {}, "The same, for the x value. Two formats because the axes are two different quantities — seconds against dollars.", false},
            PropertyInfo{"xAxis", PropertyKind::Number, "float", "20.0f", {}, "Room under the plot for the x labels. Zero draws none.", false},
            PropertyInfo{"minRadius", PropertyKind::Number, "float", "5.0f", {}, "The radii the weights are spread across, when there are weights.", false},
            PropertyInfo{"maxRadius", PropertyKind::Number, "float", "26.0f", {}, "", false},
            PropertyInfo{"fillAlpha", PropertyKind::Number, "float", "0.6f", {}, "How solid a dot is. Under one on purpose: overlapping points then read as denser rather than hiding each other, and density is most of the information in a crowded scatter.", false},
            PropertyInfo{"hitRadius", PropertyKind::Number, "float", "14.0f", {}, "How near the pointer has to be, in pixels, to pick a dot.", false},
            PropertyInfo{"tooltip", PropertyKind::Opaque, "ChartTooltip", "", {}, "", false},
            PropertyInfo{"legend", PropertyKind::Opaque, "ChartLegend", "", {}, "", false},
            PropertyInfo{"name", PropertyKind::Text, "std::string_view", "", {}, "What the chart is of — \"Revenue by month\". A chart is the one thing on a screen a reader gets nothing from: the shapes carry all of it, and the numbers behind them are the caller's. A name and a `Figure` role are the least that makes it navigable, and they are not the whole answer — see the note in `docs/reference/accessibility`.", false},
        },
        false,
        true,
        "ScatterResult scatterChart(Ui& ui, const Interaction& input, std::string_view id, const std::vector<PointSeries>& series, const ScatterOptions& options = {});",
    });
    return out;
}

}  // namespace

const std::vector<ComponentInfo>& components() {
    static const std::vector<ComponentInfo> table = build();
    return table;
}

const ComponentInfo* find(std::string_view name) {
    for (const ComponentInfo& entry : components()) {
        if (entry.name == name) return &entry;
    }
    return nullptr;
}

const std::vector<std::string_view>& groups() {
    static const std::vector<std::string_view> order = [] {
        std::vector<std::string_view> out;
        for (const ComponentInfo& entry : components()) {
            bool seen = false;
            for (const std::string_view group : out) seen = seen || group == entry.group;
            if (!seen) out.push_back(entry.group);
        }
        return out;
    }();
    return order;
}

}  // namespace gbui::meta
